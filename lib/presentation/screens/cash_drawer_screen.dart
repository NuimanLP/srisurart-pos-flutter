// CashDrawerScreen — STUB. Implementation owned by its screen agent.
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class CashDrawerScreen extends ConsumerWidget {
  const CashDrawerScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(title: const Text('เปิด-ปิดลิ้นชัก')),
      body: const Center(child: Text('เปิด-ปิดลิ้นชัก')),
    );
  }
}
