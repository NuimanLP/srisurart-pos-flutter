# Notes & Preferences

- **Styling reference:** `…/Kittasil/01_Summary/cookies-101.html`. Reuse its design language —
  warm paper (`#f4ead7`), ink (`#241309`), caramel (`#c7642d`), rust, moss, cream; fonts
  Fraunces (serif display) / IBM Plex Sans Thai (Thai body) / JetBrains Mono (labels & code);
  section headers as `§ NN — title`, bordered cards with hard offset shadow, dark "inspector"
  panels for code. Captured as a shared asset → `assets/course.css`.
- **Output = HTML**, one self-contained file per lesson, all linking `../assets/course.css`.
- Learner is bilingual Thai/EN; the app UI is Thai-first. Keep Thai labels verbatim, gloss in EN.
- Learner allowed up to 5 parallel agents for codebase research (used for the initial map).
- Audience for the *content* is a developer ("tell the dev how to config each sector").
