# ADR-0009 — ล็อกอินใหม่ทุกวัน: access 15 นาที + refresh หมดอายุตี 4

* **สถานะ:** Accepted — 2026-09-04
* **ผู้ตัดสิน:** เจ้าของโปรเจกต์
* **ปิดข้อค้าง:** `03_ARCHITECTURE.md §8` — *"JWT ต้องระบุ TTL + revoke ให้ชัด … ข้อเสนอ: access 15 นาที + refresh rotation"* (เป็นแค่ข้อเสนอ ไม่เคยมีใครเคาะ)
* **เกี่ยวข้อง:** ADR-0003 (tenant status), ADR-0004 (device roles), assignment ของอาจารย์ (JWT stateless ห้าม in-memory session)

## บริบท

เอกสารมี `/auth/token` และ `/auth/refresh` แต่ **ไม่เคยบอกอายุ token** และ §8 เขียนไว้เองว่า
"ข้อเสนอ" — ทั้งที่เป็นข้อที่อาจารย์ตรวจ และเป็นเรื่องความปลอดภัยจริงของร้าน
(ไล่พนักงานออกแล้ว token ต้องใช้ไม่ได้ — JWT เปล่า ๆ revoke ไม่ได้)

ข้อเสนอเดิมคือ refresh อายุยาว (30 วัน) + rotation เก็บใน Redis ซึ่งเป็นแพตเทิร์นเว็บทั่วไป
**แต่ไม่ได้ดูบริบทร้าน**

## การตัดสินใจ

| | อายุ |
|---|---|
| access token | **15 นาที** |
| refresh token | **หมดอายุเวลา 04:00 ของทุกวัน** (ไม่ใช่ "24 ชม. นับจากล็อกอิน") |

→ พนักงาน **ล็อกอินใหม่ทุกเช้าตอนเปิดร้าน**

## เหตุผล

1. **ร้านมีพิธีกรรมรายวันอยู่แล้ว** — เปิดกะ/ปิดกะ (`shifts`) "เปิดร้าน = ล็อกอิน" ไม่ใช่ภาระใหม่
   มันทับกับสิ่งที่ทำอยู่ทุกเช้าพอดี ต่างจาก session 30 วันที่ไม่ตรงกับจังหวะอะไรเลย
2. **เครื่องหน้าเคาน์เตอร์เป็นเครื่องสาธารณะ** ใครเดินมาก็กดได้ — ค้าง session ไว้ 30 วัน
   คือความเสี่ยงจริง ไม่ใช่ทฤษฎี
3. **revoke ง่ายลงมาก และไม่ต้องมี state เพิ่ม** — ปิด `users.is_active` แล้วรอ ≤15 นาที
   ต่อ refresh ไม่ผ่านเพราะ endpoint เช็ค `users.is_active` + `tenants.status` (ADR-0003)
   ในฐานข้อมูล → **ไม่ต้องมี denylist ใน Redis เลย** ยังตรงข้อ "JWT stateless" ของอาจารย์

### ⚠️ ทำไมต้อง "ตี 4" ไม่ใช่ "24 ชั่วโมงนับจากล็อกอิน"

แบบนับ 24 ชม. → พนักงานล็อกอินบ่ายวันจันทร์ token จะหมดอายุ **บ่ายวันอังคาร กลางที่ลูกค้ายืนรอ**
แบบตี 4 → หมดอายุนอกเวลาทำการ **ตราบใดที่ร้านไม่เปิดตอนตี 4** ต้นทุนเท่ากัน แต่เจ็บน้อยกว่าคนละเรื่อง

> ⚠️ **สมมติฐานที่ยังไม่มีเอกสารไหนยืนยัน (scrutinize 2026-09-04):** "ไม่มีวันเด้งกลางบิล" ตั้งอยู่บน
> เวลาเปิดร้านที่ไม่มีใครจดไว้ ถ้ากะที่เปิดก่อนเที่ยงคืนยังเปิดอยู่ตอน 04:15 (ตลาดนัดกลางคืน/เทศกาล)
> จะโดนล็อกอินใหม่กลางกะ และคนที่ล็อกอิน 03:55 จะได้ refresh token อายุ 5 นาที
> → เพิ่มกติกา: **refresh ที่ออกหลัง 03:00 ให้หมดอายุตี 4 ของวันถัดไป** (กัน token อายุไม่กี่นาที)
> และถามร้านตามหัวข้อ "ยังไม่เคาะ"
> "ตี 4" คิดตาม **`tenants.timezone`** (ADR-0003) เสมอ ไม่ hardcode เวลาไทย

## 🔴 ข้อขัดแย้งที่ต้องแก้ก่อนเฟส 2 (บันทึกไว้ตั้งแต่ตอนนี้ อย่าไปเจอเอาตอนนั้น)

**ถ้า token ตายตอนกลางคืน แล้วเช้ามาเน็ตล่ม เครื่องจะล็อกอินไม่ได้ = เปิดร้านไม่ได้เลย**
ซึ่ง **ล้มเจตนาทั้งหมดของการออกแบบ offline** — เราอุตส่าห์ทำให้ขายออฟไลน์ได้ แต่ดันเข้าระบบไม่ได้ตั้งแต่แรก

* **เฟส 1 ไม่เจอปัญหานี้** — ออนไลน์อย่างเดียว และไม่ cutover ร้าน (`03_ARCHITECTURE.md §7`)
  ถ้าเน็ตล่มก็ใช้ระบบไม่ได้อยู่แล้ว การล็อกอินไม่ได้จึงไม่ได้ทำให้แย่ลง
* **เฟส 2 ต้องแก้** — เครื่อง `role='pos'` (ADR-0004) ต้องยืนยันตัวตนตอนเปิดกะได้**โดยไม่ต้องมีเน็ต**
  ทางที่มีอยู่แล้วในระบบ: `users.pin_hash` (ตอนนี้ใช้เป็น manager PIN สำหรับงานเสี่ยง)
  → เก็บ hash ไว้ในเครื่องตอนซิงก์ แล้วให้เปิดกะออฟไลน์ด้วย PIN ได้ ค่อยไปต่อ token จริงตอนเน็ตกลับมา
  ~~ข้อเสนอนี้~~ **ถูกตีตก 2026-09-15 (#187)** — ดูหัวข้อถัดไป

## การเข้าระบบออฟไลน์ของเครื่อง `pos` — เคาะ 2026-09-15 (#187, เจ้าของโปรเจกต์)

**ทำไมไม่ cache `users.pin_hash`:** PIN มีแค่ 4–6 หลัก (10⁴–10⁶ แบบ) hash ที่ถูกคัดลอกลง IndexedDB
แกะได้แบบออฟไลน์โดยไม่มี rate limit ใครได้ browser profile ไปก็ได้ **manager PIN ตัวจริง** ที่ใช้ void บิลตอนออนไลน์
ไปด้วย — เปิดช่องฝั่งออนไลน์เพื่อแก้ปัญหาฝั่งออฟไลน์

**การตัดสินใจ** (รวมคำตอบของเจ้าของโปรเจกต์จาก PR #206 และ #210 — ฉบับนี้คือฉบับที่ใช้):

| หัวข้อ | ค่าที่เคาะ |
|---|---|
| ใครใช้ได้ | ~~**เฉพาะ user `role='cashier'`** บนเครื่อง `role='pos'` (ADR-0004) · **ห้ามแคช credential ของ `owner` / `manager` เด็ดขาด** — ตัดทาง privilege escalation จากการเจาะ storage ของ browser~~ — ~~**แทนที่ 2026-09-15 (#240 D12/D13):** ทุกคน (`owner` และ `staff`) ตั้งได้ แต่ออฟไลน์ทุกคนมีสิทธิ์แค่ staff~~ · **รอบ 2 (E5): 1 PIN ต่อเครื่อง `pos` สำหรับบัญชีร้าน** · ดู addendum รอบ 2 |
| credential | **PIN ออฟไลน์แยก** ไม่ใช่ `users.pin_hash` · ตั้งตอน**ออนไลน์**บนเครื่องนั้น · เก็บเป็น hash แบบช้าที่ **ผูกกับเครื่อง** (salt มาจากความลับเฉพาะเครื่อง~~คู่กับ salt สุ่มต่อ user~~ — รอบ 2 E5: PIN เดียวต่อเครื่อง) ใน Drift ของเครื่องนั้น · **ไม่เคยส่งขึ้น server** |
| อายุ | ใช้ได้ภายใน **3 วัน** นับจากการล็อกอินออนไลน์สำเร็จครั้งล่าสุด~~ของ user นั้น~~บนเครื่องนี้ (รอบ 3 F5: **ตรวจที่เครื่องเท่านั้น** นับจาก `iat` ของ `/auth/token`) |
| ใช้ได้เมื่อไหร่ | เฉพาะตอน **Degraded** (`03 §4`) — ถ้าออนไลน์ต้องล็อกอินจริงเสมอ |
| ทำอะไรได้ | เฉพาะที่ degraded mode อนุญาต (`03 §4`): ~~ขายสินค้า `offlineOk`~~ **(ยกเลิก #240 D3 — ขายได้ถ้าสต็อกในเครื่องพอ, รายการ op เต็มที่ `08 §6`)** · นับเงินในลิ้นชักในเครื่องได้ แต่ยอดสรุปและใบปิดกะทางการยืนยันหลัง sync สำเร็จ · ไม่ออก token |
| ~~ตรวจซ้ำตอน push~~ | ~~ทุก operation ใน outbox ติด `userId` + โหมดยืนยันตัวตน `offline_pin` · `/sync/push` ตรวจ: user ยัง active และ~~ยังเป็น `cashier`~~ ~~**เป็น `owner` หรือ `staff` (#240 D13)**~~ (role เดียว E1), ล็อกอินออนไลน์ล่าสุดบนเครื่องนั้นไม่เกิน 3 วันก่อนเวลาของ operation, เครื่องไม่ถูก retire · ไม่ผ่าน = `rejected` เข้าคิว reconciliation (ไม่ทิ้งเงียบ — บิลพิมพ์ไปแล้ว) · เขียน `audit_log`~~ **(รอบ 3: F5 ตัดการตรวจที่ server · F3 ผู้กระทำ = user active คนเดียวของร้าน ไม่มี `userId` ต่อ op · เครื่อง retire = token ใช้ไม่ได้ระดับคำขอ)** |
| ใส่ผิด | ล็อกในแอปหลังผิดติดกัน 5 ครั้ง — **กันได้แค่คนทั่วไปที่หน้าเคาน์เตอร์** ไม่กันคนที่ได้ storage |

~~**threat model (ยอมรับ):** คนที่ได้ storage ของเครื่องแล้วแกะ PIN ออฟไลน์ได้ จะได้แค่สิทธิ์ degraded mode ของแคชเชียร์~~
~~**บนเครื่องที่ตัวเองถืออยู่แล้ว** และทุกบิลยังถูกตรวจซ้ำตอน push แคชเชียร์ที่ถูกปิดบัญชีใช้ต่อได้ไม่เกิน 3 วัน~~
~~และบิลช่วงนั้นเข้าคิว reconciliation · ⚠️ salt ผูกเครื่องไม่ได้กันการ **แกะเลข PIN** — ถ้าแคชเชียร์ตั้ง PIN ออฟไลน์เป็นเลข~~
~~เดียวกับรหัสที่ใช้ออนไลน์ ของที่หลุดคือเลขนั้นเอง (ดู *ยังไม่เคาะ*)~~
*(รอบ 2 — threat model ใหม่อยู่ใน addendum รอบ 2 ด้านล่าง: ไม่มี "สิทธิ์แคชเชียร์" แล้ว)*

## การเซ็นและที่เก็บ token — เพิ่ม 2026-09-09 หลังทบทวนความปลอดภัย

**ปัญหาที่พบ:** ADR นี้และ `02_API_SCREENS.md §1.1` กำหนด*อายุ* token และสิ่งที่ `/auth/refresh` ต้องเช็ค
แต่**ไม่เคยบอกว่าเซ็นด้วยอะไร กุญแจอยู่ที่ไหน หมุนอย่างไร และ client เก็บ token ไว้ตรงไหน** —
#4 กำลังจะเริ่ม ถ้าปล่อยให้คนเขียนเลือกเอง ค่า default ของทุก library คือ HS256 กับ secret ก้อนเดียว
ที่ทุก process ใน compose ถือร่วมกัน

**การตัดสินใจ:**

| หัวข้อ | ค่าที่เคาะ |
|---|---|
| อัลกอริทึม | **RS256** (RSA ≥ 2048 บิต) · verifier ล็อกรายการ alg ที่รับไว้ที่ `['RS256']` เท่านั้น — ห้าม HS256, ห้าม `alg: none`, ห้ามอ่าน alg จาก header มาตัดสินใจ |
| กุญแจ | env `JWT_PRIVATE_KEY` (PEM, ตัวที่เซ็น) และ `JWT_PUBLIC_KEYS` (PEM หลายตัว key ด้วย `kid`) · **`JWT_PRIVATE_KEY` ให้เฉพาะ process ที่มี `/auth/*`** — `worker`, `migrate`, Bull-Board ต้องไม่มีตัวแปรนี้ใน compose · ไม่ commit ลง repo ไม่ฝังใน image · dev/CI ใช้ keypair ที่ script สร้างทิ้งใน `.env` |
| แยก signer/verifier ในโค้ด | `JwtSigner` (ต้องการ private key) กับ `JwtVerifier` (ต้องการแค่ public keys) เป็น provider คนละตัว · เฟส 1 ทั้งสองอยู่ใน `api-*` เดียวกัน แต่การย้าย `/auth/*` ออกเป็น service แยกต้องเป็นแค่การแก้ compose ไม่ใช่แก้โค้ด |
| หมุนกุญแจ | header `kid` ทุกใบ · ขั้นตอน: เพิ่ม public key ใหม่ลง `JWT_PUBLIC_KEYS` ทุก process → สลับ `JWT_PRIVATE_KEY` → ถอด key เก่าหลังผ่านตี 4 ไป 1 รอบ (อายุ refresh สูงสุด) · **ไม่มี JWKS endpoint ในเฟส 1** เพราะ verifier ทุกตัวอยู่ใน compose เดียวกัน |
| claim บังคับ | `iss` = `srisurart-pos` · `aud` = `tenant` / `platform` (ADR-0002) · `sub` · `iat` · `exp` · `jti` (UUID) · **`typ` = `access` / `refresh`** — guard ของ `/api/*` รับเฉพาะ `typ=access`, `/auth/refresh` รับเฉพาะ `typ=refresh` (ไม่งั้น refresh ที่เก็บใน IndexedDB ใช้ยิง API ได้ตรง ๆ) · `tid`, `role`, `did`, `drole` ตามเดิม |
| clock skew | ยอมให้ `exp` / `iat` / `nbf` คลาดได้ **30 วินาที** ไม่มากกว่านั้น |
| ที่เก็บฝั่ง Flutter Web | **access token อยู่ใน memory เท่านั้น** (reload = ขอใหม่ด้วย refresh) · **refresh token เก็บใน IndexedDB** ก้อนเดียวกับ device token (ADR-0004) · **ห้าม** เก็บ access ใน localStorage · ไม่ใช้ cookie เพราะ client กับ API คนละ origin และกติกาอาจารย์ระบุ Bearer |
| ช่องทางส่ง | `Authorization: Bearer` เท่านั้น · **ห้าม** รับ token จาก query string (จะติดใน access log ของ Nginx) |
| log | `pino-http` ต้อง redact `req.headers.authorization` และ body ของ `/auth/*` ทั้งหมด (`password`, `pin`, `deviceToken`, `code`, `refreshToken`) · เก็บ `jti` ใน log ได้ เก็บ token เต็มไม่ได้ |
| รหัสผ่าน / PIN | **Argon2id** (#4 ระบุแล้ว) พารามิเตอร์ขั้นต่ำ m = 64 MiB, t = 3, p = 1 · PIN 4–6 หลัก hash วิธีเดียวกัน แต่ entropy ต่ำ → ต้องมี rate limit ต่อ user บน endpoint ที่รับ PIN (ADR-0006) |
| เหตุการณ์ auth ลง `audit_log` | login สำเร็จ / ล้มเหลว (`auth.login`, `auth.login_failed`), refresh ถูกปฏิเสธพร้อมเหตุผล (`auth.refresh_rejected`), enrol / retire เครื่อง (`device.enrol`, `device.retire`), ตรวจ PIN ผ่าน / ไม่ผ่าน (`auth.pin_ok`, `auth.pin_failed`) — ตารางมีอยู่แล้ว (`01_DATABASE.md §5`, migration ใน #15) แต่ยังไม่มีใครเขียน · ticket แยกที่ blocked by #4 |

**ทำไม RS256 ไม่ใช่ HS256:** stack มี `api-1..3` + `worker` + Bull-Board ในเครือข่ายเดียว ถ้าใช้ HS256
ทุก process ที่**ตรวจ** token จะถือ secret ที่**ออก** token ได้ด้วย และ secret ตัวเดียวกันนั้นมักถูกวางไว้ใน
`environment:` ร่วมของ compose จน worker/Bull-Board ได้ไปด้วย — process ไหนหลุดก็ปลอมเป็นใครก็ได้
**ทุกร้าน** (multi-tenant ทำให้ blast radius เป็นทั้ง cluster ไม่ใช่ร้านเดียว) RS256 ทำให้ผู้ที่ปลอม token
ได้มีแค่ที่เดียว ยังเป็น stateless ตามกติกาอาจารย์ และเป็นคำตอบสำเร็จรูปตอนถูกถามในการนำเสนอ
ต้นทุนคือ token ยาวขึ้นราว 2 เท่า ซึ่งไม่มีผลกับ POS ที่ยิง request ไม่กี่ครั้งต่อนาที

## Addendum 2026-09-15 — phase-2 owner session #240

เจ้าของโปรเจกต์ตัดสิน D8, D11, D12, D13 ใน #240 · สเปกเต็ม [`08_PHASE2_SPEC.md §8, §13`](../08_PHASE2_SPEC.md)

| # | ตัดสิน | แทนที่ |
|---|---|---|
| ~~D12~~ | ~~บทบาทคนเหลือ **`owner` + `staff`** — `manager` หาย, `cashier` → `staff` · อำนาจ void / override วงเงินเครดิต / discard → `owner` · **เป็น slice แรกของเฟส 2**~~ | ~~`users.role` สามค่า · "manager PIN" ในเอกสารนี้หมายถึง PIN ของ `owner` ตั้งแต่ slice นั้นลง~~ **(แทนที่โดย E1/E2/E3 รอบ 2)** |
| ~~D13~~ | ~~**ทุกคนตั้ง PIN ออฟไลน์ได้** แต่ออฟไลน์ **ทุกคนมีสิทธิ์แค่ staff** · อายุ 3 วันนับจากล็อกอินออนไลน์ล่าสุดบนเครื่องนั้น · Degraded เท่านั้น · `/sync/push` ตรวจซ้ำ~~ | ~~"เฉพาะ `cashier`" และ "ห้ามแคช credential ของ owner" ในตาราง #187~~ **(แทนที่โดย E5 รอบ 2)** |
| ~~D11~~ | ~~PIN ออฟไลน์**ต้องไม่ซ้ำ** PIN ออนไลน์ — **บังคับเฉพาะ `owner`** · staff ไม่บังคับ~~ | ~~ข้อ *ยังไม่เคาะ* ด้านล่าง~~ **(แทนที่โดย E5: ต้องไม่ซ้ำรหัสผ่านออนไลน์)** |
| D8 | เน็ตกลับแล้ว user ที่เข้าด้วย PIN ออฟไลน์ **ขายต่อได้** · outbox ส่งด้วย **device token** · server ตรวจทุก op `offline_pin` · แถบขอให้ล็อกอินจริงก่อน write ออนไลน์ถัดไป · **ไม่มีการแลก JWT เบื้องหลัง** | ข้อ *ยังไม่เคาะ* ด้านล่าง |

~~**ทำไม D13 ยังรับได้ทั้งที่ #187 ห้ามแคช credential ของ owner:** เหตุผลของ #187 คือคนที่แกะ storage ได้จะได้อำนาจ owner~~
~~D13 ตัดอำนาจนั้นทิ้ง (PIN ออฟไลน์ของ owner ให้แค่สิทธิ์ staff) และ D11 ห้าม owner ใช้เลขเดียวกับ PIN ออนไลน์ ของที่แกะได้จึงไม่ใช่~~
~~PIN ที่ใช้ void ออนไลน์~~
*(รอบ 2: ไม่มีแยกสิทธิ์แล้ว — E1/E5)*

~~**ตรวจ "ไม่ซ้ำ" โดยไม่ส่ง PIN ออฟไลน์ไปไหน (08 §9.2):** owner พิมพ์ PIN ออนไลน์ → `POST /auth/verify-pin` (ถัง rate limit เดียวกับ void,~~
~~`auth.pin_ok`/`auth.pin_failed`) → ผ่านแล้วเครื่องเทียบ PIN ออฟไลน์ใหม่กับ PIN ออนไลน์**ในหน่วยความจำ** แล้วล้างทิ้ง ·~~
~~ไม่ส่ง PIN ออฟไลน์หรือ hash ของมันขึ้น server และไม่ส่ง `pin_hash` ลงเครื่อง (ทั้งสองทางเปิด oracle / ให้แกะ hash ได้) ·~~
~~ข้อจำกัด: ตรวจได้ตอนตั้งเท่านั้น~~
*(รอบ 2: E5 เทียบกับรหัสผ่าน ด้วย `POST /auth/token` ตัวเดิม — ดูด้านล่าง)*

**ข้อยกเว้นของ guard (D8):** `POST /sync/push` เป็น endpoint เดียวที่รับ **device token** (`X-Device-Token`) แทน access token —
guard ของ `/api/*` อื่นยังรับเฉพาะ `typ=access` ตามหัวข้อ *"การเซ็นและที่เก็บ token"* · `userId` ใน op เป็นคำอ้างของเครื่อง จึง
~~**op ในคิวไม่มีอำนาจของ owner** (08 §6.4 — รอเจ้าของยืนยันใน 08 §15 Q3)~~ *(รอบ 2: มี role เดียว)* · log ต้อง redact `X-Device-Token`

## Addendum 2026-09-15 (รอบ 2) — owner round 2 on #240 (E1, E2, E3, E5) + review PR #254

สเปก [`08_PHASE2_SPEC.md §3, §8, §13`](../08_PHASE2_SPEC.md) · **E ชนะ D**

| # | ตัดสิน | แทนที่ |
|---|---|---|
| E1 | role คน **`owner` เดียว** · ทุกแถวเดิมเป็น `owner` · guard ที่เช็ค role คนหายหมด | D12 (`owner`+`staff`), `manager`/`cashier` |
| E2 | **บัญชีร้านบัญชีเดียว** · `audit_log.user_id` = บัญชีร้าน · "ใคร/ที่ไหน" = `did` | – |
| E3 | void ออนไลน์ **ไม่มี PIN มีแต่เหตุผลบังคับ** · ลบ manager PIN + rate limit ของมัน · `users.pin_hash` ไม่มีใครใช้ → ลบ | manager PIN (รวม `auth.pin_ok`/`auth.pin_failed` ในตาราง audit) |
| E5 | **1 PIN ออฟไลน์ต่อเครื่อง `pos`** สำหรับบัญชีร้าน · 3 วัน · Degraded เท่านั้น · ~~ตรวจซ้ำตอน push~~ (F5: เครื่องเท่านั้น) · **ต้องไม่ซ้ำรหัสผ่านออนไลน์** | D11, D13, ตาราง #187 แถว "ใครใช้ได้" |

**ตรวจ "ไม่ซ้ำรหัสผ่าน" (08 C4):** หน้าตั้ง PIN ให้พิมพ์รหัสผ่านอีกครั้ง → `POST /auth/token` ตัวเดิม (rate limit + audit มีแล้ว) → ผ่านแล้วเทียบ `PIN != รหัสผ่าน` ในหน่วยความจำ แล้วล้างทิ้ง · ไม่ส่ง PIN/hash ขึ้น server · ตรวจได้ตอนตั้งเท่านั้น

~~**เวลาล็อกอินออนไลน์ล่าสุด (08 C5):** `devices.last_online_login_at` เขียนโดย `/auth/token` — เขียนไม่ได้ = ล็อกอินไม่สำเร็จ (แถว `auth.login` ใน `audit_log` เขียนแบบกลืน error จึงใช้เป็นหลักฐานไม่ได้ `auth.service.ts:404-411`) · server ตรวจ `date ≤ last_online_login_at + 3 วัน + 5 นาที` ไม่ตรวจขอบล่าง (นาฬิกาคลาด)~~
*(รอบ 3 F5: ไม่มีการตรวจที่ server — ไม่มี `devices.last_online_login_at`)*

**ข้อยกเว้นของ guard (D8, คงเดิม):** `POST /sync/push` รับ device token (`X-Device-Token`) แทน access token — endpoint เดียว · ~~บัญชี `is_active` ตรวจทุก op ไม่สน `authMode`~~ *(รอบ 3 F3: user `is_active` คนเดียวของร้านเป็นผู้กระทำ ไม่มี `authMode`)*

**threat model (รอบ 2, ยอมรับ):** คนที่ได้ storage + device token ของเครื่อง `pos` ปลอม op ได้ทุกชนิดที่เข้าคิวได้ (ขาย คืน ลิ้นชัก เปิดกะ ชำระเครดิต ลูกค้า override วงเงิน void บิลออฟไลน์) · กันด้วย (1) `/retire` ถอน token (2) void ออฟไลน์ได้เฉพาะบิลที่ขายออฟไลน์ (3) override วงเงิน / void ออฟไลน์ / กะไม่นับเงิน / วันที่ผิด ลงรายการให้ owner ตรวจ · ของที่แกะได้จาก PIN ออฟไลน์ไม่ใช่รหัสผ่านออนไลน์ (E5)

## Addendum 2026-09-15 (รอบ 3) — owner round 3 on #240 (F3, F5, F6, F9)

สเปก [`08_PHASE2_SPEC.md §3, §8.1, §13`](../08_PHASE2_SPEC.md)

| # | ตัดสิน | แทนที่ |
|---|---|---|
| F3 | ผู้กระทำใน `/sync/push` = **user `is_active` คนเดียวของ tenant** หาที่ server — ไม่มาจาก body · ใช้กับ `actor.userId` และทุกแถว `audit_log` (CHECK `user_id IS NOT NULL`) | `userId` ต่อ op |
| F5 | หน้าต่าง 3 วันของ PIN ออฟไลน์ **ตรวจที่เครื่องเท่านั้น** · นับจาก `iat` ของ token จาก `/auth/token` (refresh ออก `iat` ใหม่ ไม่นับ) · ไม่มี `devices.last_online_login_at`, `authMode`, `OFFLINE_PIN_REJECTED` | การตรวจซ้ำที่ server (#187 / รอบ 2) — `date` มาจากเครื่อง จึงกันคนปลอมไม่ได้ ได้แต่ตีกลับบิลจริง |
| F6 | retire / enrol / export ต้องมี `did` (ล็อกอินพร้อม device token ที่ enrol แล้ว) · รหัสผ่านอย่างเดียวไม่พอ · เครื่องแรกใช้ enrolment code ของ provisioning | "`owner` เท่านั้น" ของ #163 (บัญชีร้านเดียว = ทุกคนรู้รหัส) |
| F9 | **user active หนึ่งคนต่อร้าน** (`uq_users_one_active`) · ร้านเดิมที่มีหลายคน: เก็บคนแรก ที่เหลือ `is_active=false` | – |

## ผลที่ตามมา

* `02_API_SCREENS.md §1.1` ต้องระบุอายุ token ทั้งสองตัวให้ชัด ไม่ใช่ปล่อยว่าง
* `/auth/refresh` **ต้องเช็คทั้ง `users.is_active`, `tenants.status` และ `devices.retired_at` ของ
  `did` ใน token** ทุกครั้ง (ไม่ใช่แค่ตรวจลายเซ็น) — ข้อสุดท้ายเพิ่ม 2026-09-04: ถ้าไม่เช็ค
  เครื่อง `pos` ที่ owner กด "ย้ายเครื่องขาย" (ADR-0004) จะยังต่อ token ที่มี `drole='pos'` ได้
  จนถึงตี 4 = ร้านมีเครื่องขาย 2 เครื่องทั้งวันโดยที่ `one_pos_per_tenant` ช่วยอะไรไม่ได้
  ช่องที่เหลือหลังแก้คือ ≤15 นาที (อายุ access token) เท่ากับกรณีปิด `users.is_active`
* ไม่ต้องทำ refresh-token rotation + Redis denylist ตามข้อเสนอเดิม — **ตัดงานออกไปหนึ่งก้อน**
* ต้องมีข้อความไทยตอน session หมดอายุ — 🔴 **ห้ามแต่งเอง** (กติกา `02_API_SCREENS.md §8.1`)
* (2026-09-09) #4 ต้องทำตามหัวข้อ *"การเซ็นและที่เก็บ token"* ทั้งตาราง — `config.ts` เพิ่ม
  `JWT_PRIVATE_KEY` / `JWT_PUBLIC_KEYS` เป็น required เฉพาะ process ที่มี `/auth/*`, compose และ
  `server.yml` ต้องมี keypair สำหรับ dev/CI, e2e ต้องมีเคส "token ที่เซ็นด้วย HS256 โดยใช้ public key
  เป็น secret ถูกปฏิเสธ" (การโจมตี key-confusion แบบคลาสสิก) และ "refresh token ยิง `/api/*` ถูกปฏิเสธ"
* (2026-09-09) เหตุการณ์ auth ทั้งหมดต้องลง `audit_log` — ticket แยกที่ blocked by #4 (ดูตารางด้านบน)
  งานที่แตะเงิน/สต็อกใน #16, #20–#28 ต้องเรียก writer ตัวเดียวกันใน transaction เดียวกับ business write

## ยังไม่เคาะ

* [x] ~~ตี 4 ตาม timezone ของ tenant~~ — **เคาะแล้ว 2026-09-04:** ใช้ `tenants.timezone` เสมอ
* [ ] **ถามเจ้าของร้าน:** มีวันไหนที่ร้านยังขายอยู่ตอนตี 4 ไหม และพนักงานที่เปิดร้านรับได้ไหมกับการ
      ล็อกอินใหม่ทุกเช้า — ถ้าคำตอบข้อแรกคือ "มี" ต้องเปลี่ยนเวลาตัดเป็นค่าต่อร้าน (`tenants.session_reset_at`)
* [ ] เครื่อง `backoffice` ควรอายุสั้นกว่าเครื่อง `pos` ไหม (มือถือ/โน้ตบุ๊กหายง่ายกว่าเครื่องที่ตั้งอยู่กับที่)
* [x] ~~อัลกอริทึมเซ็นและที่เก็บ token~~ — **เคาะแล้ว 2026-09-09:** RS256 + `kid`, access ใน memory,
      refresh ใน IndexedDB (หัวข้อ *"การเซ็นและที่เก็บ token"*)
* [x] ~~เปิดร้านตอนเน็ตล่มหลัง token ตาย~~ — **เคาะแล้ว 2026-09-15 (#187):** PIN ออฟไลน์แยก ~~เฉพาะ `cashier`~~ (~~ทุกคน สิทธิ์ staff ตั้งแต่ #240 D13~~ → 1 PIN ต่อเครื่อง E5), ใช้ได้ภายใน
      3 วันนับจากล็อกอินออนไลน์ล่าสุดบนเครื่องนั้น (หัวข้อ *"การเข้าระบบออฟไลน์ของเครื่อง `pos`"*)
* [x] ~~PIN ออฟไลน์ต้องห้ามซ้ำกับรหัส/PIN ที่ใช้ออนไลน์ไหม (ถ้าซ้ำ การแกะ hash ในเครื่องได้เลขที่ใช้ออนไลน์ด้วย)~~ —
      **เคาะแล้ว 2026-09-15 (#240 D11/D13):** บังคับเฉพาะ `owner` เทียบกับ PIN ออนไลน์ (addendum ด้านบน) ·
      ~~ยังค้าง: owner ที่ไม่มี `pin_hash` และต้องห้ามซ้ำรหัสผ่านด้วยไหม → `08 §15 Q5`~~ **รอบ 2 (E5): เทียบกับรหัสผ่าน ไม่มี PIN ออนไลน์แล้ว**
* [x] ~~ตอนเน็ตกลับ แคชเชียร์ที่เข้าด้วย PIN ออฟไลน์ต้องล็อกอินออนไลน์ใหม่ หรือแลกเป็น JWT เบื้องหลัง — PR #206 เสนอแบบหลัง
      แต่ในเครื่องไม่มี credential ของ user ที่ server ตรวจได้ (device token ยืนยันตัว**เครื่อง** ไม่ใช่ตัวคน) ต้องออกแบบก่อนทำ~~ —
      **เคาะแล้ว 2026-09-15 (#240 D8):** ขายต่อได้ ส่ง outbox ด้วย device token ไม่แลก JWT · ~~การตีความ "ขายต่อ" รอยืนยัน `08 §15 Q4`~~ (08 §13: op ยังเข้า outbox จนล็อกอินจริง)
* [x] **เคาะแล้ว 2026-09-15 (#240 D7 → E4 รอบ 2):** void ออฟไลน์ได้**เฉพาะบิลที่ขายออฟไลน์** ต้องใส่เหตุผล · ลงรายการให้ owner ตรวจ ไม่มี undo (`08 §12`) · ข้อความเดิม: ~~void บิลตอนออฟไลน์ — ไม่ได้เคาะในรอบนี้ (#206 เสนอ "ห้าม" เจ้าของโปรเจกต์ยังไม่รับ) · ข้อเท็จจริงวันนี้: void ต้องตรวจ
      manager PIN ที่ server และบิลต้องอยู่ในกะที่เปิดของเครื่อง (#94) จึงทำออฟไลน์ไม่ได้อยู่แล้วจนกว่าจะออกแบบ~~
* [ ] เฟส 2: refresh ที่นอนอยู่ใน IndexedDB ของเครื่อง `pos` ตลอดคืน ควรผูกกับ device token
      (`refresh` ใช้ได้เฉพาะเมื่อส่งคู่กับ `deviceToken` ตัวเดิม) ไหม — ยังไม่จำเป็นในเฟส 1 เพราะออนไลน์อย่างเดียว
