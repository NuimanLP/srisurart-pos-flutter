# Handoff — เคลียร์บั๊กชุด #363–#367, ปิด 3 ใบ, ซ้อมเดโมบนเครื่อง dev (2026-09-21)

> รอบนี้เป็น **orchestration**: session หลักกระจายงานให้ agent 8 ตัว (Opus/Sonnet) แล้วรวมผล
> ทุกใบเดินตาม `09_PHASE2_LANES.md §10` — `/scrutinize` แนวทางก่อน → เขียนโค้ดตาม
> `karpathy-guidelines` → ปิดด้วย `/code-review`
>
> **จุดยืนของรอบนี้: ไม่ติ๊ก AC ด้วยคำบอกเล่า** ทุกช่องที่ติ๊กมี file:line / PR / output จริงกำกับ
> ส่วนที่พิสูจน์ไม่ได้ถูกปล่อยเปิดพร้อมเหตุผล

## 1. เข้า `main` แล้ว 8 PR

| PR | ใบ | ผล |
|---|---|---|
| #374 | #364 `ownerPassword` ไม่มีเกณฑ์ความยาว | **ปิด** — เกณฑ์ย้ายไปอยู่ `src/common/password.ts` ที่เดียว (`MIN_PASSWORD_LENGTH=12`), `createTenant` ปฏิเสธด้วย `WEAK_PASSWORD` **ก่อน hash และก่อนเปิดธุรกรรม**, argon2 ย้ายออกนอกธุรกรรม |
| #373 | #367 CORS/`PLATFORM_ADMIN_IPS` ไม่ถึง container | **ปิด** — ส่งผ่าน anchor `x-app-env`; ค่าที่ตั้งแต่ไม่มี entry (`,`) **throw ตอน boot** แทน fallback เงียบเป็น `'*'` |
| #371 | #366 ระบอบ deploy ขัดกับ D9 | **ปิด** — auto-deploy คงไว้ แต่ผ่าน GitHub environment `demo` ที่ต้องมีผู้อนุมัติ (ตั้ง `required_reviewers` จริงแล้ว) |
| #372 | #363 backup ไม่เคยออกนอก VM | กลไก `offsite_upload()` ผ่าน `rclone` (ยังไม่ผูก creds) |
| #377 | #363 (แก้ตามมติเจ้าของ) | offsite = **optional**: ไม่ตั้งค่า → warning + **exit 0**; ตั้งค่าแล้วล้ม → error + exit ≠ 0 |
| #375 | — | เอกสารรอบซ้อมเดโมบน dev + GIF + สคริปต์พูด 12 ขั้น |
| #376 | #364 | เจ้าของรับรองสตริงไทย `WEAK_PASSWORD` (ลบกำกับ `agent ร่าง`) |
| #378 | #184 | runbook k6 + RSS ฉบับเดินตามได้จริง (อ้างอิง ไม่ปิดใบ) |

## 2. การตัดสินใจของเจ้าของในรอบนี้ (ทั้งหมดลงวันที่ 2026-09-21)

1. **ระบอบ deploy** = auto ทุก green `main` **แต่ต้องมีคนกดอนุมัติ** ที่ environment `demo`
   (ADR-0013 มี addendum แล้ว) ⇒ ตั้งแต่นี้ merge เข้า `main` จะมี deployment ค้าง *Waiting for review*
2. **offsite backup เป็นฟีเจอร์ optional** — ไม่บังคับว่าต้องตั้งค่า
   🔴 ผลข้างเคียงที่ต้องจำ: **`backup-cron.log` เขียวไม่ได้แปลว่ามีไฟล์ออกนอก VM** ต้องอ่านบรรทัด
   `::warning::`/`::error::` และยังไม่มีใครถูก page เมื่อมันเตือน
3. **รับรองสตริง** `รหัสผ่านไม่ผ่านเกณฑ์ ต้องมีอย่างน้อย 12 ตัวอักษร` (ตรวจ md5 ตรงกันทั้ง 3 ที่)
4. **reopen #288** เพราะ AC "ไฟล์ backup ออกนอก VM อัตโนมัติทุกวัน" ยังไม่เคยเป็นจริง
5. **#184**: ให้เขียน runbook ละเอียด + คอมเมนต์เสนอ body ใหม่ถึงเจ้าของใบ — **ไม่สร้างใบใหม่**

## 3. ผลการตรวจใบเก่า (close-out audit)

ไม่มีใบไหนปิดได้ทั้งใบ — ติ๊กเท่าที่มีหลักฐาน:

- **#338** ติ๊ก 4/5 · เหลือ AC3 เพราะชุดคำสั่งยังไม่เคยรันบน `mob04`
- **#346** ติ๊ก 2/4 (AC2, AC4) · AC1 **premise กลับหัวแล้ว** (PR #356/#361 แก้ไปก่อน) ต้องแก้ข้อความใบ
  · AC3 ต้องรัน `provision.yml` บน VM
- **#184** ทั้ง 4 AC ยังว่าง — PR #357 เขียน `Closes #184` ทั้งที่ส่งแค่ tooling + runbook
- **#345** สองข้อที่ค้างยังค้าง: `PattaraponKitcharoen` **ยังไม่เคยคอมเมนต์ที่ใดในรีโป** และ #184 ยังไม่มีอะไรให้ติ๊ก
- **#365** ทุก AC ติด VM · ตรวจฝั่งโค้ดแล้ว: ความเสี่ยงสองข้อ (repair branch ของ `deploy.yml`,
  `ETCD_ROOT_PASSWORD` ที่อบใน volume) **ถูก document ครบ** แต่ **วิธีรีเซ็ตรหัสโดยไม่ลบข้อมูลยังไม่มี
  ลำดับคำสั่งที่รันได้จริง** — บันทึกว่า "ขาด" ไม่ได้แต่งขึ้น
- **`03_ARCHITECTURE.md §8`**: ตัวเลขจริงวันนี้คือ **17 ช่อง ติ๊ก 16 เหลือ 1** (ช่อง k6 ที่ `:55`)
  — `CLAUDE.md` ที่เขียนว่า "5 of 13" ล้าสมัย **แก้แล้วในรอบนี้**

## 4. ซ้อมเดโมบนเครื่อง dev — ผ่านครบ 7 ขั้น

รายละเอียดเต็ม + GIF + สคริปต์พูด: [`demo-rehearsal-dev-2026-09-21.md`](demo-rehearsal-dev-2026-09-21.md)

ผลย่อ: login page ขึ้นจริง → ขายบิล `RC01-2569-09-0001` → `sales` 0→1 → ส่งซ้ำคีย์เดิมไม่เกิดบิลที่สอง
(ตรวจ sha256 ของ body ให้ตรง `request_hash` ก่อนยิงซ้ำ) → Grafana success 100% / p95 169 ms /
error-by-status มี 403 / replays 1.01

🔴 **ไม่ติ๊ก AC ของ #335/#343/#344 แม้ข้อเดียว** — เดโมบน `mob04` ยังไม่เคยเกิด

กับดักที่ต้องรู้ก่อนขึ้นเวที: ตั้ง font scale **0.85x** (ปุ่มชำระเงินตกใต้จอ) · กด cert
*Advanced → Proceed* ล่วงหน้า (extension คลิกผ่านหน้าเตือนไม่ได้) · **ขายเฉพาะ `DEMO-001`**
อย่าจิ้มไทล์ seed ของ Drift · `docker compose` ต้องใส่ `-f` ให้ครบชุดเสมอ

## 5. เหลืออะไร — ทั้งหมดต้องมี VPN หรือคนจริง

- 🔒 **#343 → #344 → #345** — deploy จริง → เดโมครบวง → reconcile
- 🔒 **#365** etcd auth บน VM · **#363/#288** ต้องเลือกปลายทาง + สร้าง creds + ติดตั้ง `rclone` บน `mob04`
- **#184/#251** k6 สามเครื่อง (lane C) — runbook พร้อมแล้วที่ [`ticket-184-k6-rss-runbook.md`](ticket-184-k6-rss-runbook.md)
- **#67** runner ยังไม่ติดตั้ง · **CD ยังถูก FortiGate บล็อกที่ GHCR** (ไม่ใช่ปัญหาของรีโป)
- **#231** cutover (ปลายทางเฟส 2)

## 6. คำถามที่ยังรอเจ้าของ

1. **#346 AC1** ข้อความกลับหัวแล้ว — จะแก้ถ้อยคำหรือปล่อย?
2. อยากได้ **ใบใหม่แยกของ lane C** สำหรับ k6+RSS ไหม (ตอนนี้ใช้ #184 เดิม + runbook)
   — ถ้าสร้างใบใหม่ต้องตัดสินใจเรื่องยุบ #184 ซึ่งเป็นใบของเลนอื่น
3. agent ของ #184 เติมบรรทัด *superseded* ไว้บนหัวไกด์ `slice-22-…` ของ lane C
   (เปิดเผยไว้ในคอมเมนต์แล้ว) — ถอนได้ถ้าไม่เหมาะ
4. เมื่อ offsite เปิดใช้งานวันแรก: dump ทุกไฟล์จากช่วงที่ยังไม่ตั้งค่าไม่มี marker
   ⇒ จะถูกเก็บเกิน retention ตลอดไป ต้อง upload-or-delete ด้วยมือหนึ่งรอบ
