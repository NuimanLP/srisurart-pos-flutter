# handoff_log — index

1 บรรทัดต่อ handoff · เรียงใหม่ → เก่า · รูปแบบตาม [`handoff-prompt-template.md`](handoff-prompt-template.md)

- 2026-09-22 — [Handoff — รอบตัดสินใจของเจ้าของ: k6, offsite backup, และการเคลียร์ใบ](session-2026-09-22-owner-decisions.md) — ไม่มีโค้ดเปลี่ยนสักบรรทัด ตอบคำถามที่ค้างท้าย handoff 21/09 §6 แล้วบันทึกลงใบ/เอกสาร · **#251 ปิด — ยืนยันมติเดิม 2026-09-15 ไม่กลับลำ**: 3 เครื่อง แต่ละเครื่องใต้ `perip` ของตัวเอง **ไม่มีข้อยกเว้นให้ `perip`** (เกือบเลือก carve-out ใน `nginx.conf` แล้วพบว่า `03 §8.1` ตอบไปแล้วและมีโค้ด remote-write/htpasswd ลงไปแล้ว — บทเรียน: ค้นก่อนว่าคำถาม "owner decision needed" เคยถูกตอบหรือยัง) · **เปิด #380** ของเลน C เหลือเฉพาะการวัด **ไม่ reopen #184** (บันทึกตามจริงว่าเจ้าของปิดเองเมื่อ 21/09 ทั้งที่ AC ทั้ง 4 ยังว่าง) · **ปลายทาง offsite = NAS ที่ร้าน ไม่ใช่คลาวด์** แต่ 🔴 **ผลค้นคว้าหักล้าง SFTP: BeeStation รัน BSM ไม่ใช่ DSM ไม่มี SSH/SFTP ให้ใช้** เหลือสองทาง (rclone backend `smb` บนเครื่องเดิม / ซื้อ DSM DS-series) — [`research-363-sftp-nas-offsite.md`](research-363-sftp-nas-offsite.md) · dump เก่าที่ไม่มี `.uploaded` marker ให้ upload ย้อนหลังด้วยมือรอบเดียวตอนเปิดใช้ ไม่เขียน backfill อัตโนมัติ · แก้ถ้อยคำ #346 AC1 ที่ premise กลับหัว · ลด "superseded" บนไกด์ของเลน C เป็น "see also" · **ไม่ติ๊ก AC ของ #363/#288/#346 เลย — มติไม่ใช่หลักฐาน**

- 2026-09-21 — [Handoff — เคลียร์บั๊กชุด #363–#367, ปิด 3 ใบ, ซ้อมเดโมบนเครื่อง dev](session-2026-09-21-clear-bugs-and-demo-rehearsal.md) — รอบ orchestration: กระจายงานให้ agent 8 ตัว แล้ว merge 8 PR เข้า `main` — **ปิด #364 / #366 / #367** (นโยบายรหัสร่วมฟังก์ชันเดียว + ปฏิเสธก่อน hash/ก่อนธุรกรรม · CORS/`PLATFORM_ADMIN_IPS` ถึง container แล้วและค่าที่ตั้งแต่ว่าง throw ตอน boot แทน fallback เงียบเป็น `'*'` · auto-deploy ผ่าน environment `demo` ที่ต้องมีผู้อนุมัติ) · **offsite backup เป็น optional ตามมติเจ้าของ** (ไม่ตั้งค่า = warning + exit 0 · ตั้งแล้วล้ม = error + exit≠0) 🔴 `backup-cron.log` เขียวไม่ได้แปลว่ามีไฟล์ออกนอก VM · **reopen #288** · รับรองสตริงไทย `WEAK_PASSWORD` · runbook k6+RSS สำหรับ lane C · **ซ้อมเดโมครบ 7 ขั้นบนเครื่อง dev พร้อม GIF** · close-out audit: #338 ติ๊ก 4/5, #346 ติ๊ก 2/4 (AC1 premise กลับหัว), #184 ทั้ง 4 AC ยังว่างทั้งที่ PR #357 เขียน `Closes`, `03 §8` จริง ๆ คือ **17 ช่อง ติ๊ก 16 เหลือ 1** ไม่ใช่ "5 of 13" · 🔴 ทุกอย่างที่เหลือเป็น VPN/HITL: #343 → #344 → #345, #365, #363/#288, #184/#251

- 2026-09-21 — [Ticket #184 `close.3` — the executable runbook for the two ACs that are left](ticket-184-k6-rss-runbook.md) — #184 was closed by PR #357 (`Closes #184`, 4 files, +409/-0) which shipped **tooling + a runbook only**: no deploy, no RSS table, no k6 p95 — reopened, all four ACs still `[ ]`. This is the step-by-step replacement for lane C: 10 prerequisites each with the read-only command that proves it (P8 = probe `/prometheus-remote-write/api/v1/write` from each laptop, **401 = allowed, 403 = outside nginx's allowlist, stop**), the three-laptop `SHARD=i/3` procedure run **one scenario at a time on a barrier** (`run-shard-test.sh` runs all four back-to-back and desynchronises the machines — not the submission tool), the RSS sampler at an explicit `1800` s (its 600 s default stops mid-`DURATION=10m`) with the restart counters snapshotted **before** the run because `RestartCount` is cumulative, the AC3/AC4 tables to fill, and what must **not** be recorded as a pass. 🔴 Corrects the #357 runbook's `BASE_URL=… pnpm k6:setup`, which cannot work from a laptop: `setup.ts` needs a direct Postgres connection + `redis-cache` + `JWT_PRIVATE_KEY` and the VM publishes no datastore port — the only proven path is close3's SSH tunnels, and the container IPs must be re-read because Postgres/Redis sit in the dynamic `ip_range`. AC1/AC2 belong to #343 (blocked on the FortiGate/GHCR block) and `03 §8`'s k6 box stays unticked because **#251** still owes a decision: three laptops cap at ~72 r/s aggregate, so §9's 1,000/500/200-VU load column is not what this run proves. `References #184`, ticks nothing.

- 2026-09-21 — [ซ้อมเดโม #335 บนเครื่องผู้พัฒนา (rehearsal on dev)](demo-rehearsal-dev-2026-09-21.md) — เดินลูปเดโม D1–D10 ครบทั้งเจ็ดขั้นบน `5b16d05`: `bootstrap:admin` → platform token → provision tenant `srisurart-rehearsal` (`enrolCode=CE7DA8B3`) → build web โหมด server (`USE_API_WRITES=true` + `API_BASE_URL=` ว่าง = same-origin) → ผูกเครื่อง/login/เปิดกะ/ขาย 1 บิล → `sales` 0→1 → ส่งซ้ำ `Idempotency-Key` เดิม = ไม่เกิดบิลที่สาม + `pos_idempotency_replay_total` 0→1 → Grafana (success 100%, p95 169ms, error-by-status มี series 403, replays 1.01) · มี GIF + สคริปต์พูดเดโม 12 ขั้น + ตาราง "อะไรต่างบน mob04" · สะดุด 9 จุดพร้อมทางเลี่ยง (BuildKit ชน reparse point ของ BeeStation · nginx บน dev ไม่มี volume ของเว็บ · extension คลิกผ่านหน้าเตือน cert ไม่ได้ · ปุ่มชำระเงินตกใต้จอ) · 🔴 **ไม่ติ้ก AC ของ #335/#343/#344 แม้ข้อเดียว — เดโมบน `mob04` ยังไม่ได้ทำ** — ซ้อมเสร็จ รอรอบ VM

- 2026-09-21 — [Ticket #363 `ops.backup-offsite` — mechanism built, not wired](ticket-363-backup-offsite.md) — `backup-db.sh` never copied a dump off the VM although #288's own AC for it was still unticked on a closed issue (#288 was reopened by the owner the same day). Added a pluggable `offsite_upload()` via `rclone` (`BACKUP_RCLONE_REMOTE`/`BACKUP_RCLONE_CONFIG`, both unset = disabled — owner decision, no real credentials wired this session) covering all three destinations #363 AC1 names (Supabase storage / S3-compatible / SFTP-to-lab-machine) through one code path. 🔴 **Corrected later the same day (owner decision, branch `fix/363-offsite-optional`): offsite upload is OPTIONAL** — unconfigured (`BACKUP_RCLONE_REMOTE` unset) is one `::warning::` line and **exit 0** so the nightly cron is quiet-but-honest; only a *configured but broken* destination (no `rclone`, missing config, failed `copyto`) is a loud `::error::` + non-zero exit, which is the half #363 exists for. A green `backup-cron.log` therefore does not prove a backup left the VM. Local prune only deletes a dump once its offsite copy is confirmed (`.uploaded` marker) once offsite is actually turned on, and is unchanged while it stays off (no unbounded disk growth during an indefinite "not wired yet" period). Proved with 4 dry-run scenarios against a stub `rclone` + fake local destination (disabled / binary missing / upload fails / upload succeeds — prune of confirmed-vs-unconfirmed old backups verified in the last one). Commented on #288 to correct the record instead of ticking anything. `References #363`, not `Closes` — destination choice, real credentials, a real off-VM upload and a real restore all remain owner/VM-only and unproven.

- 2026-09-21 — [Handoff — ปิดชุดใบงานเดโม #335 ทั้งสามเลน และ CD ถูกบล็อกที่ firewall ของคณะ](handoff_demo-335-merge-and-cd-blocked_21_09_2026.md) — merge 11 PR ปิดงานทั้งสามเลนเข้า `main` `825a497`, ปลดบล็อกเกอร์ `.env` บน `mob04` (คีย์ `K6_REMOTE_WRITE_BASIC_AUTH_*` ที่ #251 เพิ่มทีหลังและ `:?` ทำให้ `docker compose pull` ตายก่อน pull), แล้ว deploy จริงล้มที่ **FortiGate ของคณะทำ SSL deep inspection** ส่งใบรับรอง `O=Fortinet, CN=FG3K4ETB19900078` ที่ไม่มี SAN แทน ghcr.io ⇒ CD ตายทั้งเส้น D9 และเส้น self-hosted runner และติดตั้ง CA ก็แก้ไม่ได้ · บันทึกกับดักที่เสียเวลาจริงรอบนี้: `--delete-branch` ปิด stacked PR, Python text mode บน Windows ทำไฟล์เป็น CRLF จนชนทั้งไฟล์, `grep -c` ไม่ anchor `^` ทำให้คีย์ที่ติดท้ายบรรทัดอื่นดูเหมือนมีอยู่, `--check` ของ `deploy.yml` ให้ false positive เรื่อง network · **สถานะ: ติดอยู่ รอฝ่ายเครือข่ายยกเว้น ghcr.io — VM ยังรัน `8e873cd` ไม่ถูกแตะ**

- 2026-09-21 — [Ticket #343 `vm.deploy` — D9 pre-flight for the first real deploy to `mob04`](ticket-343-vm-deploy.md) — read-only survey of `mob04` (running `8e873cd`, 188 commits behind `main`) plus the D9 six-item pre-flight with evidence: **5 clear, 1 blocker** — `/opt/pos/.env` predates #251 and lacks the two `K6_REMOTE_WRITE_BASIC_AUTH_*` keys that `server/docker-compose.yml:103-104` requires with `:?`, so every Compose task would die at interpolation until `provision.yml` is re-run · the network already carries `ip_range` so 07 §7's one-time outage is **not** needed · `provision.yml` needs `DEMO_SSH_USER=cloud` (sudo) while `deploy.yml` needs `deploy` (owns `/opt/pos`, in `docker`) and the two are not interchangeable · Ansible runs from a digest-pinned container because a Windows bind mount is world-writable and silently voids `ansible.cfg` unless `ANSIBLE_CONFIG` is passed · copy-paste command sequence for deploy → `/health/ready` → `.current_sha` → a **separate** Grafana check → rollback drill → return. **No AC ticked: nothing was deployed.**

- 2026-09-21 — [Ticket #346 `ops.backup-scripts` install the ops scripts the VM is told to run](ticket-346-backup-script-install.md) — two rounds. Round 1: `provision.yml` creates `/opt/pos/scripts` and installs `backup-db.sh` as `deploy:deploy` mode `0755` before the daily cron; records that #288 supplied only offline/static evidence. Round 2: the copy becomes a `loop` over the three scripts whose runtime **is** the VM — `restore-db.sh` (#288's recovery half) and `measure-container-rss.sh` (#184 step 2.2) were never copied anywhere, and the #184 runbook invoked the sampler as `./deploy/scripts/…` from `/opt/pos`, a path that does not exist there · the cron redirect `>/dev/null 2>&1` becomes `>>/opt/pos/backups/backup-cron.log 2>&1` · read-only evidence from `mob04` **inverts AC1**: `crontab -l -u deploy` is empty and `provision.yml --check --diff` shows `@@ -0,0 +1,2 @@`, so the backup job was never scheduled at all rather than failing nightly in silence. `References #346` — AC1 needs the owner's call, AC3 needs `provision.yml` to run on the VM.

- 2026-09-21 — [Ticket #338 `platform.provision` provision tenant โดยไม่แตะ psql](ticket-338-platform-provision.md) — ใบเอกสารล้วน (ไม่แก้โค้ด): พิสูจน์ `POST /api/v1/platform/tenants` จากใน netns ของ nginx บนสแตก dev เต็ม ได้ `enrolCode` จริง แล้วเดินต่อจนเจ้าของร้าน login ได้ (`/auth/device` → deviceToken → `/auth/token` → accessToken ที่มี `tid` ใหม่) · ตรวจ ADR-0001 ข้อ 4/5 ด้วย psql: owner role=owner · settings tax 7/30 วัน · 5 หมวด · เครื่อง `pos1` no=1 role=pos · audit `platform.tenant.create` ในธุรกรรมเดียว · 🔴 วัดเหตุผลที่ `ssh -L` ใช้ไม่ได้เป็นตัวเลข: ยิงจากโฮสต์ได้ 403 และ log บอก `client: 172.30.0.1` โดย `upstream` ว่าง (nginx ปฏิเสธเองเพราะไม่มี `set_real_ip_from`/`real_ip_header`) · 🔴 `enrolCode` คืนครั้งเดียว (เก็บแค่ sha256, หมดอายุ 7 วัน) · กับดัก BusyBox `wget` ต้องใช้ `--post-file` · `nginx.conf` ไม่แก้ `PLATFORM_ADMIN_IPS` ไม่ตั้ง · AC ขา VM **ปิดครึ่ง** รอ VPN
- 2026-09-21 — [Ticket #337 `admin.bootstrap` คำสั่งเดียวได้ platform admin คนแรก](ticket-337-admin-bootstrap.md) — `pnpm bootstrap:admin` (`server/src/db/bootstrap-admin.ts`, `export bootstrapAdmin()` + entry guard แบบ ESM) สร้าง/รีเซ็ต platform admin ด้วย `hashPassword` (argon2id ตัวเดียวกับ `platform-auth.service.ts`) แล้วล็อกอิน `POST /api/v1/platform/auth/token` ได้จริง · เลี่ยงกับดัก D2 ครบสาม: เรียก `createMigrationDataSource` โดยไม่เอ่ย `DataSource` → `tenant-door.spec.ts` เขียวโดยไม่แก้ spec/allowlist · `BOOTSTRAP_ADMIN_DISPLAY_NAME` เป็นอินพุตเพราะ `display_name` NOT NULL ไม่มี default · ตรวจ owner role ด้วย `pg_has_role` ไม่ใช่แกะ URL (เพราะ `pos_app` มี GRANT INSERT บน `platform_admins` จริง) · 🔴 รันซ้ำแม้ `--force` **ไม่ปลุก** admin ที่ `is_active=false` — ตั้งใจ · e2e 6 เคสจบที่ HTTP login ทุกเคส · VM ใช้ `docker compose run --rm -e BOOTSTRAP_ADMIN_… migrate node dist/db/bootstrap-admin.js` (ยังไม่เคยรันบน `mob04` — รอ VPN)


- 2026-09-20 — [📋 STATUS BOARD ชุดใบงานเดโม #335 (สามเลนขนานกัน)](demo-335-STATUS.md) — กระดานสถานะที่ทั้งสามเลนเข้าไปอัปเดตเอง: ตารางใบ #336–#345 พร้อมสถานะ/PR/หลักฐาน · ประกาศข้ามเลน (ใหม่→เก่า) · บล็อกของแต่ละเลนพร้อมอาณาเขตไฟล์และข้อเตือนจาก D4–D10 · กฎการแก้ไฟล์ร่วม (แก้เฉพาะบล็อกตัวเอง เติมบนสุด commit แยก rebase ก่อน push ห้ามติ๊ก AC ที่ยังไม่ได้รันจริง) · คู่กับแผนแบ่งงาน [demo-335-three-agent-split.md](demo-335-three-agent-split.md)

- 2026-09-20 — [Slice 22 (#184) Container RSS Measurement & Distributed k6 Load Test Guide](slice-22-k6-rss-measurement-guide.md) — ระบบวัดและบันทึกหน่วยความจำ Memory RSS ต่อคอนเทนเนอร์ภายใต้โหลดจริงบน VM `mob04` (`deploy/scripts/measure-container-rss.sh`) พิสูจน์เพดาน 6.0 GB host ceiling ตาม `03_ARCHITECTURE.md §8` และ `08_PHASE2_SPEC.md §17` · สคริปต์รัน distributed load test `server/test/k6/run-shard-test.sh` แบ่ง 3 เครื่อง (`SHARD=i/3`) ป้องกันชน per-IP rate limit (0 `429` errors) พร้อมคำสั่ง `k6:verify` ยืนยัน data integrity

- 2026-09-20 — [Slice 25 (#67 `cd.2-run`) Self-Hosted Runner on mob04 Guide & Automated Installer](slice-25-cd2-runner-guide.md) — ติดตั้งและตั้งค่า self-hosted runner บน VM `mob04` (`172.30.58.20`) ตามข้อบังคับ `07_CICD_DEPLOY.md §6.2` และ `ADR-0013` · จัดทำสคริปต์ติดตั้งอัตโนมัติ `deploy/scripts/setup-mob04-runner.sh` ที่รันคำสั่งเดียวสร้าง user `gha-runner` (ไม่มี docker group), ตั้งค่า `/usr/local/bin/pos-deploy` และ hook `runner-job-started.sh`, ตรวจสอบ sudoers ด้วย `visudo -cf`, ดาวน์โหลดและคอนฟิก runner unattended พร้อม systemd service · จัดทำคู่มือและ checklist ยืนยัน 3 AC (push main auto deploy, ปฏิเสธ workflow นอก main/fork, fail auto-rollback run ยังแดง)

- 2026-09-20 — [Ticket #336 `env.secrets` สุ่ม secrets ครบชุด แล้วสแตกขึ้นได้จากคลีนโคลน](ticket-336-env-secrets.md) — สุ่มค่าลง `server/.env` (dev) และเนื้อหาสำหรับ `DEMO_ENV_FILE` ของ VM ทั้งสองไฟล์อยู่นอก git · ตารางคีย์ที่ compose บังคับด้วย `:?` ครบ 10 ตัว + คีย์ที่ default ไว้แต่ต้องมี (`BULL_BOARD_USER`, `GRAFANA_ADMIN_*`) + คีย์ที่ห้ามใส่ (`IMAGE_TAG` — Ansible ส่งเอง) · dev คง `dev-only-*` ไว้สามคีย์เพราะ e2e แปดไฟล์ hardcode เป็น default (สุ่มทับ = `pnpm test:e2e` ของทุกเลนตายเงียบ) VM สุ่มใหม่หมด · บันทึกกับดัก volume: `pgdata`/`etcd-data`/`nginx-auth` อบ secret ไว้ตอน bootstrap ครั้งแรก เปลี่ยน `.env` ทีหลังไม่มีผล · พบช่องว่าง `CORS_ORIGINS`/`PLATFORM_ADMIN_IPS` ไม่ถูกส่งเข้า container โดย compose ใด ๆ (รายงานที่ #335) · พิสูจน์คลีนโคลนด้วย project name ทิ้ง (`-p`) ไม่แตะ volume ของสแตกหลัก — 15 service ขึ้นครบ, `/health/ready` เขียว, etcd RBAC + htpasswd + cert ตรวจแยกเพราะ `/health/ready` ไม่แตะ etcd

- 2026-09-20 — [Ticket #212 Slice 13b `fe.pull-cursor` Keyset Pull Sync & Stock Protection](ticket-212-pull-cursor.md) — ระบบ Keyset Pull Sync บน Flutter: Drift Schema bump v10 → v11 (`sync_cursors`) พร้อม codegen และ migration · ซิงค์ข้อมูล `products`, `customers`, `mechanics` โดยใช้ cursor ล่าสุดจาก server (`meta.nextCursor`) เท่านั้น (ไม่คำนวณจากแถวในเครื่อง) · หน้าแรกถอย 30 วินาที (`updatedSince = cursor - 30s`) และละเว้น `afterId` · Stock Protection (ADR-0010 §D3 / 08 §15): คงสต็อกในเครื่องสำหรับสินค้าที่มี op ค้างใน `outbox_ops` ไม่ให้ server เขียนทับ · กรอง deleted rows และ `import-tombstone` ออกจากรายการเลือก · `SyncService` ดึง pull อัตโนมัติหลัง push เสร็จในสถานะ online · Full test suite 533/533 ผ่านครบ 100%

- 2026-09-20 — [Ticket #276 Slice 11-c `q2.void-client` offline void handling on soldOffline bills](ticket-276-q2-void-client.md) — ระบบยกเลิกบิลออฟไลน์บน Flutter: Drift Schema bump v9 → v10 (`Sales.soldOffline`, `Sales.voidReason`) พร้อม migration และ build_runner · `SalesRepository.voidSaleOffline` คืนสต็อกและย้อน ledger ลูกค้า/ช่างใน transaction เดียว · `ApiSalesRepository.voidSaleOffline` บังคับ `soldOffline == true` (ปฏิเสธบิลออนไลน์ด้วย `VOID_NEEDS_ONLINE` ตาม 08 §12) + บันทึก op `sale.void_offline` ลง `outbox_ops` · หน้า `ReturnsScreen` แสดงปุ่ม "✕ ยกเลิกบิลออฟไลน์" เฉพาะบิล `soldOffline` ในโหมด Degraded พร้อม dialog บังคับกรอกเหตุผล (ซ่อนปุ่มบนบิลออนไลน์) · Unit & Widget tests 9/9 ผ่านครบ

- 2026-09-20 — [Ticket #288 Slice 23 `ops.backup` automated database backup & recovery](ticket-288-ops-backup.md) — ระบบสำรองฐานข้อมูล PostgreSQL รายวันด้วย `pg_dump --create` + gzip + SHA256 checksum + retention 7 วัน · รักษากฎความปลอดภัย Transaction Ceiling (#213) ต่อท้ายคำสั่งตั้งค่า Role Timeout ให้ `pos_app` (25s/5s) ทำให้ `DbModule.warnIfRoleTimeoutsDiffer` หลัง restore ไม่ส่งเสียงเตือน · สคริปต์ `restore-db.sh` ตรวจ checksum และ assert ค่า role timeouts · Ansible provision โฟลเดอร์ `/opt/pos/backups` (0700) + daily cron 03:00 น. · e2e test suite 6/6 ผ่านครบ

- 2026-09-18 — [Architecture Primer & Concurrency Locking / Caching Deep Dive](session-2026-09-18-architecture-primer-concurrency-caching-deepdive.md) — จัดทำเอกสารปูพื้นฐานสถาปัตยกรรม `architecture-primer.md` (997 บรรทัด ครบ 13 หัวข้อ §0–§12) และสเปกอ้างอิง `architecture.md` ใน `docs/Backend_design/` ตามแม่แบบ `primer-template.md` · เจาะลึก Concurrency & Locking เปรียบเทียบ Optimistic (Retry Storm) vs Pessimistic (Pool Starvation) vs Distributed In-Flight Lock vs Atomic Decrement และ Lock Hierarchy แก้ Deadlock `40P01` · เจาะลึก Caching & Invalidation (Cache-Aside, Write-Through, Write-Behind, Stock Overlay Pattern, Single-Flight Promise Memoization, TTL Jitter) · ผ่านเกณฑ์ 11 ข้อของ primer-template 100% ครบ 2 รอบ

- 2026-09-17 — [ปิด Phase 1 (#185) และเริ่ม Phase 2 (#268 Option A)](session-2026-09-17-phase1-close-phase2-start.md) — ปิด #185 นำเข้า snapshot จริงผ่าน checklist 6 ข้อ (e2e 44/44 checks ผ่าน 100%, ติ๊ก DoD ใน 03 §8) · ปิด #268 (Phase 2 slice 0c) เคาะ Option A ข้อความไทย 5 error codes + 13 UI จุด, อัปเดต server_error_resolver.dart, dart analyze + test ผ่านคลีน · สรุปแผน Phase 2 เตรียมเริ่ม #269 (slice 0d sync.seam)

- 2026-09-15 (ค่ำ) — [รอบ orchestrator ปิดงาน phase 1](orchestrated-closeout-round-2026-09-15.md) — merge 13 PR (#235 #236 #237 #244 #246 #247 #250 #252 #253 #255 #256 #257 #260)
  - เจ้าของเคาะ: #67 self-hosted runner · #185 ใช้ข้อมูลสังเคราะห์ก่อน · #238 tombstone + ทิ้งแถวซัพพลายเออร์กำพร้า · #251 k6 หลายเครื่อง → Prometheus · #239 import เป็น background job
  - 🔴 etcd auth ไม่เคยเปิดบน demo VM (#250) · watch ถือ token หมดอายุค้าง เพราะ watch ตอบ 200 ไม่ใช่ 401 (#255) · ลิ้นชักที่ import มาค้างเปิดถาวร (#244)
  - ⚠️ subagent รัน `docker compose down -v` บนเครื่อง dev → volume Postgres/Redis ของเครื่องนี้หาย
  - #239 ปิดแล้วด้วย PR #260 · ค้าง: #67 ต้องติดตั้ง runner และรันจริง · deploy รอบหน้าต้องเช็ก etcd auth · #251/#184 วัดจริงด้วย 3 เครื่อง · #185 รอไฟล์จริง

- 2026-09-15 — [#185 `close.4`: ลอง checklist §9 ด้วย snapshot สังเคราะห์](close4-synthetic-snapshot-2026-09-15.md) — เจ้าของโปรเจกต์สั่งให้ใช้ข้อมูลสังเคราะห์ก่อน เพราะยังไม่ได้ไฟล์จริงจากร้าน
  - สร้าง generator แบบ deterministic ที่จำลองการขายของร้านทีละวันตามกฎของ Drift repository (สินค้า 320 · บิล 2,043 · 4 เดือน · 2 MiB) · commit เฉพาะ sample เล็ก 142 KiB
  - Flutter: `importLegacyBackup()` ผ่าน 4/4
  - Server: รอบแรกบน `main` พัง
    - 🔴 body เกิน 100 KiB ได้ 500
    - 🔴 import อ่าน key ที่แต่งขึ้นเอง (`sa_purchase_orders`/`sa_shifts`/`sa_parked_sales`) → PO, กะ, ลิ้นชัก, บิลพัก หายหมด แต่ยังตอบ 201 · หมวดกลายเป็น `Cat-n` · `zone` ไม่ถูกแปลง
  - แก้แล้ว: 6 ข้อใน §9 ผ่านครบ · rollback ผ่าน · closing report ถูกต้อง · บิลแรกหลัง import ได้เลข `RC01-2569-09-0001`
  - 🔴 กับดัก: ถ้าส่ง `json()` ให้ `app.use` ตรง ๆ Nest จะข้าม parser ของตัวเอง ทุก route อื่นเลยไม่มี body
  - 🔴 ไฟล์แบบ realistic (มีสินค้า/ลูกค้า/ช่างที่ถูกลบจริงแต่ประวัติยังอ้างถึง) → FK ได้ 500 → #238 · ช่องโหว่ pre-flight ที่เหลือ → #239
  - #185 ยังเปิดไว้ รอไฟล์จริง

- 2026-09-15 — [#184 `close.3` deploy จริงครั้งแรกบน demo VM + rollback + k6 บน VM](close3-demo-deploy-2026-09-15.md) — `provision.yml` เพิ่ม `JWT_PLATFORM_SECRET` · `deploy.yml` `8e873cd` ผ่าน (`/health/ready` 200, `.current_sha` ตรง) · rollback `4f3a244` → กลับ `8e873cd` (102 / 95 วินาที, health ไม่หลุด) · 200 `POST /sales` บนสต็อก 50 → 50 บิล 0 5xx สต็อก 0 (`k6:verify` ผ่าน) · 🔴 latency §9 ยังวัดไม่สะอาด: nginx `perip` 30r/s ทำให้ k6 จากเครื่องเดียววัด nginx · 🔴 etcd auth ไม่เปิดบน VM (`etcd-init.sh` กลายเป็นโฟลเดอร์ — แก้ใน branch `fix/etcd-init-deploy`) · 🔴 rollback ถอยแค่ image ไม่ถอย config · `/health/ready` ตอบ postgres down ตอน pool เต็ม · `verify-integrity.ts` ผ่านแม้ขาย 0 บิล — #184 เปิดไว้ให้เจ้าของปิด

- 2026-09-15 — [Lane A: ปิดงานค้าง phase 1 + เคาะ ADR phase 2](lane-a-closeout-round-2026-09-15.md) — orchestrator + agent (implement → `/code-review` + `/scrutinize` แยก agent → แก้ → merge) · merge #197 (#183 timeout) #198 (#182 scheduler) #204 (#188 doc-counters, schema v6) #205 (#201 builtin jitter) #215 (#213 commit guard 25 วินาที) · รีวิวย้อนหลัง #208/#209 (ไม่ต้อง revert) · #187/#191 เคาะแล้ว (#214 แทน #206/#210) · 🔴 role timeout 5 วินาทีทำให้ report พัง → เปลี่ยนเป็น commit guard · 🔴 test ต้องรันแบบไม่มี env ค้าง · 🔴 checkout หลักใช้ร่วมกับอีก session · ค้าง #184–#186 #217 #219–#221 และติ๊ก DoD — กำลังทำ

- 2026-09-14 — [#25 Ticket 25 `p6.1` GET /bootstrap + GET/PATCH /settings](ticket-25-p61-bootstrap-settings.md) — เอนด์พอยต์ GET /bootstrap คำนวณ Strong ETag SHA256 ตอบ 304 Not Modified เมื่อข้อมูลไม่เปลี่ยน + GET/PATCH /settings ตรวจสิทธิ์ manager — ปิดแล้ว
- 2026-09-14 — [#66 `ops.3` runtime config via etcd v3](ticket-66-ops3-etcd-runtime-config.md) — `RuntimeConfigService` ใน NestJS อ่านและ watch `/pos/config/log_level` ผ่าน etcd v3 gRPC-gateway HTTP API ด้วย native `fetch` (ไม่พึ่งพา `etcd3` npm) · mutate `logger.level` แบบ real-time โดยไม่ต้อง restart app · fail-open ปลอดภัย fallback ไปใช้ `LOG_LEVEL` จาก `.env` ทันทีเมื่อ etcd unreachable — เสร็จสมบูรณ์พร้อม merge

- 2026-09-14 — [#67 `cd.2` auto-deploy & rollback workflow](ticket-67-cd2-auto-deploy.md) — GitHub Actions workflow `.github/workflows/deploy.yml` (workflow_run trigger หลัง Server CI & Flutter CI สำเร็จบน main + workflow_dispatch rollback) · pre-flight check ตรวจ release images บน GHCR ผ่าน `verify-ghcr-tags.sh` (exit 0 กัน race condition) · concurrency group deploy-demo (ไม่ cancel กลางคัน) · Ansible deploy release บน environment demo — เสร็จสมบูรณ์พร้อม merge

- 2026-09-13 — [#65 `cd.1` Ansible provision & deploy base](ticket-65-cd1-ansible-deploy.md) — Playbooks `provision.yml` (Docker Engine, UFW 22/80/443, deploy user, /opt/pos/ setup) + `deploy.yml` (idempotent SHA check, GHCR pull, web-sync volume update, pre-code DB migration, rolling restart api-1..3, health check) · Compose override `deploy/compose/vm.override.yml` (GHCR images, remove build:, web volume mount) · validation script `deploy/scripts/validate.sh` — เสร็จสมบูรณ์พร้อม merge

- 2026-09-13 — [#44 `sec.1` backend security gate & hardening](ticket-44-sec1-security-hardening.md) — Helmet headers (nosniff, frameguard, CSP-free API) + dynamic CORS · rate limit brute-force บน login (`POST /auth/token`) และ manager PIN (`POST /sales/:id/void`) ตอบ 429 `RATE_LIMITED` พร้อม `Retry-After` · negative-path e2e 20 เคส ครอบคลุม OWASP Top 10 (claim spoofing, SQL injection, JWT forge/alg:none/expiration, misconfig, brute-force) · pin GitHub Actions commit SHA ใน server/flutter workflows + เพิ่ม CodeQL workflow — เสร็จสมบูรณ์พร้อม merge

- 2026-09-13 — [#83 frontend ServerErrorResolver heuristic + unmapped codes](ticket-83-server-error-resolver.md) — ปรับ `_containsThai` เป็น `_startsWithThai` ป้องกัน regex mismatch · แมพ 8 canonical error codes ค้างท่อ · 264 Flutter tests ผ่านครบ — ปิดแล้ว (merge)

- 2026-09-13 — [#37 `p11.1` k6 load testing & data integrity proof](ticket-37-p111-k6-loadtest.md) — รัน k6 load test ผ่านครบทั้ง 4 scenarios (Read 1000 VUs cache hit 99.99%, Contention 200 VUs 50 created / 150 refused / stock 0, Idempotency 100 VUs x 5 100 created / 400 matched, Mixed 500 VUs 37,160 reqs p95 14.8ms) + พิสูจน์ data integrity ด้วย verify-integrity.ts — ปิดแล้ว (merge)

- 2026-09-13 — [#24 ต่อ: ไม่มีกะ ไม่รับเงิน · flush ก่อนปิดกะ · แก้ regression ของ #92](lane-a-24-credit-payments.md) — เจ้าของโปรเจกต์ตัดสิน AC3: **1ง** ปิดกะ = ส่ง outbox ก่อน ค้างเงินสด = ปิดไม่ได้ (แถวส่งช้าเคยตกกะถัดไป) · **2ข2** `POST /sales` + `POST /mechanics/:id/credit-payments` → `409 NO_OPEN_SHIFT` ทุกวิธีจ่าย (`requireOpenShiftIdFor` + `FOR SHARE` กันบิลแข่งกับการปิดกะ · replay ยังผ่าน) + client เช็คกะใน Drift ก่อนเขียน outbox · **3** เช็คจ่ายเกินหักยอดรอส่ง (ร้านมีเครื่อง `pos` เครื่องเดียว ADR-0004 → ตัวอย่าง "อีกเครื่องรับไปแล้ว" ผิด สาเหตุจริงคือยอดบนจอไม่หักคิว) · **4ก** ปิด #24 ย้าย AC3 ไป #30 · 🔴 **เจอ regression ของ #92: build Drift ของร้านใช้ `ApiMechanicsRepository` (`useApiRepositories` = true) → รับชำระเข้าคิวค้างตลอด ยอดไม่ลด** → flag `writesToServer` ผูก `useApi` · contract test หยุดที่ `@override` · 2 agent Opus (server/client) · 247 → 263 tests · e2e 225/227 (2 แดงเดิมบน `main`) · 🔴 ค้าง: `POST /returns` ยังไม่เช็คกะ · `closeForRetirement` ข้าม flush · ปิด #24/คอมเมนต์ #30 รอสิทธิ์ GitHub — ปิดแล้ว (merge)

- 2026-09-13 — [#24 `p5.7` รับชำระเครดิตช่าง + outbox ฝั่ง client](lane-a-24-credit-payments.md) — `POST /mechanics/:id/credit-payments` (`pos` only · idempotent · เลข CP · migration `…005` `payment_method` + `shift_id`) · 🔴 จ่ายเกิน = `409` เว้นแต่ `allowOverpayment` + audit (ไม่ clamp เงียบ) · review รอบ 3 (Scrutinize/Spec = Opus · Standards = Sonnet): 🔴 **B1 transport failure ตกไป Drift** → ถ้าคำขอไม่ถึง server `syncFromServer` เขียนหนี้เต็มกลับทับ (เงินหาย) · 🟠 **M1 id/key อยู่ในหน่วยความจำ** ทั้งที่เอกสาร 4 จุดอ้างว่ารอดรีสตาร์ต · 🟠 **M2 resend ไม่พก `allowOverpayment`** → แก้ทั้งหมดด้วย **outbox ใน Drift schema v5** (`pending_credit_payments`: id + key + body เก็บก่อนส่ง) = รับชำระตอนออฟไลน์ได้ · 4xx ระหว่าง flush → banner ให้คนตัดสิน · mutation check ยืนยันเทสต์ · 243 → 247 tests · 🔴 ค้าง: AC3 รอ #30 · แถวส่งช้าอาจตกกะถัดไป · sales/returns/shifts ยังมีปัญหาแบบ M1 — ปิดแล้ว (merge)

- 2026-09-12 — [รอบตรวจ 3 แกนบน #56/#82 แล้ว merge PR #84](review-merge-fe3-84.md) — CI ที่แดงบน #84 ไม่ใช่ของ branch (sha ที่พังเป็น merge commit ของ GitHub ตัว p95 ของ #29 ทำ `main` แดงด้วย) · conflict เดียวตอน merge `main`: สวิตช์ของ #55 กับ #56 — เก็บทั้งคู่ (เลือกข้างเดียว = อีกสไลซ์ตายเงียบ) · ตรวจ 3 แกน (Standards/Spec/Scrutinize) → 🔴 **4 ตัว ทั้งหมดคือการถือว่า server ตอบแล้วทั้งที่ยังไม่ได้ตอบ**: `ApiException` ทุกตัวถูกนับเป็น verdict รวม 5xx/429 (nginx 504 → บิลที่สอง) · `createReturn`/`addDrawerEntry` ไม่กันซ้ำเลย (คืน  3 จาก 10 สองครั้ง = 600 บาทสำหรับของ 300) · `409 CREDIT_LIMIT_EXCEEDED` ไม่มีตัวรับ = ทางตัน · ปิด attempt ก่อน patch → `PendingWrites` + `isVerdict` + `PosException` · 🔴 แถมของ #55: fallback 16 จุด รัน Drift transactional service ซ้ำหลัง server ตอบแล้ว (contract test มองไม่เห็นเพราะ glob แคบ) · #54: `onSessionExpired` ไม่มีคนฟัง · 223 → 239 tests · ปิด #56 #82 #33 #34 · #54/#55 เปิดไว้ (**#54 ปิดไม่ได้เพราะแอปยังไม่มีหน้า login เลย**) · 🔴 ค้าง: default ของสองสวิตช์ยังไม่สอดคล้องกัน · **#30 ขึ้นกับ #24 จริง แต่ tracker ไม่ได้บันทึก** — ปิดแล้ว
- 2026-09-12 — [#28 `p6.3` ลิ้นชักเงินสด + `shift_id`](p6.3-shifts-drawer.md) — ใบสุดท้ายของ Lane A ที่หยิบได้เอง · 🔴 **ตรวจก่อนลงมือแล้วพบว่า behaviour มาครบตั้งแต่ PR #75** (`src/shifts/` + endpoint 5 ตัว + แสตมป์ `shift_id`) issue ค้างเพราะ AC "ทุก sale **และ return** ถือ `shift_id`" รอ #22 เท่านั้น → เหลือ assertion 2 จุด: กะที่ลืมปิดต้องเห็นใน `GET /shifts/history` ขณะที่ `GET /shifts/current` ตอบกะใหม่ · ใบลดหนี้ตอนไม่มีลิ้นชักเปิดต้องได้ `shift_id` null · audit map เคส Dart ↔ e2e ได้ 10/10, code-review 2 แกนไม่มี blocker · unit 97 / e2e 152 · 🔴 baseline แดง 14 เคสเพราะ DB เครื่องตามหลัง migration `…004` ขณะที่ `db:migrate:status` บอก "up to date" (dist เก่า) · 🔴 เคส `200 concurrent bills` วัดสาเหตุได้แล้ว = `pg-pool` timeout ใน middleware ก่อน routing (ของ `tx.*` ไม่ใช่บั๊ก sale path) · 🔴 `closeForRetirement` ยังไม่มีคนเรียกจริง รอ slice device — ปิดแล้ว
- 2026-09-11 — [#22 `POST /returns` + #23 void กลับรายการ ledger](lane-a-22-23-returns-void.md) — ปิด Lane A ที่ปลดล็อกแล้วสองใบใน PR #78 · #22 พอร์ต `returns_repository.dart` ครบ (over-refund guard ข้อความไทยตรงตัว · กลับส่วนลด/แต้ม/ยอดช่างตามสัดส่วน · `credit_balance` ขยับเฉพาะ `หักจากเครดิต` · คืนครบ → void บิลแม่) + migration `…004` `return_items.cost_at_sale` + เขียน `returns.shift_id` (ปลดล็อก #28) + `GET /returns` · #23 ทำ AC 3 ข้อเดียวที่ค้าง (AC 1/2/4/5 มาตั้งแต่ #75) · 🔴 lock order ยาวขึ้น **sale → mechanic → products → doc_counters → customer** · 🔴 **blocker ที่ scrutinize จับได้: ยอดคืนเป็นราคาที่ client ส่งมา ไม่เคยเทียบบิล — ออกใบลดหนี้ 999,999 กับบิล 85 ได้ และ `GREATEST(0, …)` กลบให้เงียบด้วยการกดหนี้ช่างเป็น 0** → `409 RETURN_PRICE_MISMATCH` · แก้อีก 5 (บรรทัดซ้ำคนละราคา · ของ soft-delete คืนไม่เหมือนกันสองทาง · `หักจากเครดิต` บนบิลไม่มีช่าง · `GET /returns` ผิดสัญญา §314 · เอกสาร 4 จุด) · 🔴 เจอบั๊ก fixture: `tenants.code` ตัดจาก uuid 8 ตัวแรก 3 suite ชนกัน · unit 97 / e2e 149 — ปิดแล้ว
- 2026-09-11 — [#11 ตัดสิน `total_credit` → #21 ledger effects + sync board กับ commit](lane-a-21-ledger-effects.md) — #11 = parity: `total_credit` คือชื่อเก่าของ `total_discount` จาก JS (ป้าย "ลดให้ช่าง" บนหน้าจอช่าง) server ไม่เขียน · #21 PR #76: customer points/spend · mechanic tab/สถิติ · `409 CREDIT_LIMIT_EXCEEDED` + `overrideCreditLimit` + audit · 🔴 lock order ใหม่ **mechanic → products → doc_counters** (#22 #23 ต้องตาม) · 🔴 e2e pin ว่า resend key เดิม + flag = 201 (`tx.3` ต้องรักษา) · scrutinize + code-review 2 แกน แก้ 5 จุด · unit 85 / e2e 120 · board: #15 #38 ย้ายไป team/1 ตาม author, parent checklist ติ๊กครบ, #3 ปิด, PR 13 ใบได้ assignee — ปิดแล้ว
- 2026-09-11 — [เคลียร์ branch เข้า `main`: merge #74 #75 #73 + รอบตรวจก่อน merge](branch-cleanup-merge-73-74-75.md) — ตรวจ 3 PR ขนาน (Opus scrutinize + code-review 2 แกน / Sonnet) · #75 แก้ก่อน merge 4 ตัว: `items[].price` ติดลบผ่าน, ADR-0003 addendum ห้ามโค้ดที่ตัวเองส่ง → **เสนอ**, void เขียน `movements.type='return'` → migration `'void'`, init script ไม่มี exec bit · ลบ branch ที่ merge แล้ว 11 อัน เหลือ `main` + POC · open PR = 0 — ปิดแล้ว
- 2026-09-10 — [รีวิว 3 รอบ #53 + เทสต์ v1 → v3 + merge #58/#51](review-merge-schema-v3.md) — `/code-review` 2 แกน + `/scrutinize` บน branch #53 · จับได้ว่าเทสต์ migration คุมแค่ v2 → v3 ทั้งที่ DB ร้านอาจเป็น v1 → เพิ่มเทสต์ v1 → v3 ด้วย DDL ตัวจริง (135 → 139 tests) · ยืนยัน codegen แบบ build เย็น · merge #58 (`ded95ef`) + #51 (`ab2312d`) เข้า `main` · 🔴 ไม่เพิ่ม `Products.deletedAt` ตาม ADR-0010 ข้อ 2 ยกไป #55 — ปิดแล้ว
- 2026-09-10 — [รอบตรวจ Lane A: blocker idempotency · audit ทำ pool แตก · ADR-0003 → handler-scoped](lane-a-review-and-adr0003.md) — ตรวจ PR #75 สามแกนขนาน (Standards/Spec/Scrutinize) แล้วแก้ 8 ตัว · 🔴 **blocker: `Idempotency-Key` ซ้ำบน `POST /sales/:id/void` ตอบ 200 พร้อมบิลผิด สต็อกไม่คืน** เพราะ fingerprint ใช้ `req.route.path` ที่เป็น pattern · 🔴 audit ตอน void ถูกปฏิเสธขอ connection ที่สองจาก pool เดียวกัน → cashier คนไหนก็ทำให้ request อื่น 500 ได้ (`403,403,500,500`/5013ms → `403×4`/47ms) · replay บิล void → `409 SALE_VOIDED` · ปิดกะซ้ำได้ code ของตัวเอง · `paymentMethod` whitelist 3 ค่า (เจ้าของโปรเจกต์ชี้ขาด เอกสารกับหน้าจอขัดกัน `'บัตร'` ไม่มีจริง) · fixture เลิกใช้ `tester` ซ้ำทุก tenant · **ADR-0003 แก้แล้ว → handler-scoped** พร้อมแผน `tx.0`–`tx.5` (ยังไม่ลงมือ) · 🔴 e2e ทน runner ที่สองบน DB เดียวกันไม่ได้ — รอ merge
- 2026-09-10 — [Lane A: #19 เลขเอกสาร · #20 POST /sales · #23 อ่านบิล+void · #28 ลิ้นชัก](lane-a-sales-shifts-void.md) — ticket ที่ปลดล็อกได้ทั้งหมดของ `team/1` รอบเดียว + seam ที่ #4 ทิ้งไว้ (guard ไม่เคยทำ `SET LOCAL` → เขียน endpoint ไม่ได้เลย) · 200 บิลพร้อมกันบนสต็อก 50 → 50 บิลเป๊ะ · scrutinize 3 agent เจอ blocker 4 ตัว (pool deadlock, connection leak, backstop แข่ง handler, retire เครื่องแล้วยอดวันนั้นหายจาก API) แก้หมด · 🔴 เจอบั๊กบน `main`: `SET LOCAL app.tenant_id = $1` รับ bind param ไม่ได้ → **auth audit log ไม่เคยเขียนสำเร็จเลยตั้งแต่ #4** · 🔴 **ไม่ปิด #23/#28** เพราะ AC ติด #21/#22 · PR #75 เขียวครบ — รอ merge
- 2026-09-10 — [grill รอบ 3: CI/CD + deploy design → ADR-0013, 07, spec #60, #61–#67](cicd-design-grill.md) — 7 บล็อก DevOps ของคอร์ส → GitHub Actions (ไม่ Jenkins) · Trivy image gate บล็อก push GHCR · web image static-only + volume · Ansible auto-deploy ขึ้น VM `demo` + rollback ด้วย SHA · etcd 1 key · Prometheus/Grafana loopback · status job PR-only filter ปิด #39/#40 AC4 · 🔴 พบ Nginx `/platform/` allowlist ไม่เคยทำงาน (prefix `api/v1`) → #62 · CONTEXT.md เกิดแล้ว · ลงมือแล้ว #61 → #70, #62 → #69 (ผ่าน code-review 2 แกน) image ทั้งสองบน GHCR และ public ตั้งแต่ push แรก (#71 แก้ runbook) ที่เหลือ #39, #63–#67 เพื่อนทำ — ปิดแล้ว
- 2026-09-10 — [#5 p3b tenant provisioning, platform admin plane, status lifecycle, snapshot import](p3b-tenant-provisioning.md) — Platform admin realm auth, dual DataSource isolation, atomic provisioning, snapshot importer with pre-flight scan, status lifecycle & Redis purge, audit logging — ปิดแล้ว
- 2026-09-10 — [ตัด issue frontend (`q1`) + #53 Drift schema v3](fe0-drift-schema-v3.md) — ตัด #52 parent + #53–#56 (schema v3 ย้ายมา `team/1`) · #53 ทำจบ: `Sales.shiftId`, `Shifts.id` TEXT (rebuild ด้วย `TableMigration` + CAST), `Products.offlineOk`, `products.updatedAt` เขียนจริงครบ 6 path · 123 → 135 tests · พิสูจน์ migration บน Chrome (`sharedIndexedDb`) ด้วย · 🔴 กับดัก: extension ที่อ้างชนิดจาก `database.g.dart` ห้ามอยู่ใน `database.dart` ไม่งั้น codegen ตายเงียบ — PR #58 เขียว รอ merge
- 2026-09-09 — [Lane A: #40 image artefact + #18 idempotency](lane-a-ci3-idempotency.md) — #40 merged (`2a5d377`, artefact ออกจริงบน `main` 63.6 MB + 16.1 MB) · #18 PR #51 เขียวรอ merge (unit 8 / e2e 26) · 🔴 ถอด Trivy image scan เพราะ `node:22-alpine` มี 13 fixable HIGH/CRITICAL ของตัวเอง → #44 · 🔴 ADR-0003 ทำด้วย guard เดียวไม่ได้ #4 ต้องเป็น split 3 ท่อน — #40 ปิดแล้ว / #18 รอ merge
- 2026-09-09 — [ปิดรูรั่วใน compose: Redis AUTH + ไม่ publish port ของ datastore](compose-hardening-redis-auth-ports.md) — `--requirepass` ทั้งสอง Redis (รหัสไปกับ `REDIS_*_URL`, ไม่แก้โค้ด) · เอา `ports:` ของ Postgres/Redis ออกจาก compose หลัก ย้ายไป `docker-compose.dev.yml` สำหรับ dev+CI เท่านั้น · PR #49 เขียวครบ 4 job · 🔴 ต้องเติม `REDIS_PASSWORD` ใน `.env` เดิม — ปิดแล้ว

- 2026-09-09 — [ทบทวนความปลอดภัย: JWT signing, audit log, CVE/OWASP](security-review-jwt-audit-cve.md) — ADR-0009 addendum (RS256 + `kid`, `typ` claim, access ใน memory) · #43 `audit_log` writer · #44 `sec.1` · job `audit`/`deps-audit` ใน CI · multer override (3 high CVE) · OWASP Top 10 ลง `04_QA` รอบ 4 · 🔴 Dependabot ตั้งผิดรอบแรก เปิด PR #45–#48 ต้องปิดทั้งหมด — ปิดแล้ว
- 2026-09-08 — [ปฏิเสธ CouchDB — คง PostgreSQL](couchdb-rejected.md) — เจ้าของโปรเจกต์ตัดสินคงสถาปัตยกรรมเดิม → ADR-0012 Rejected + บันทึกเหตุผล, ถอน banner freeze ทุกไฟล์ · #4–#37 กลับมาหยิบได้ — ปิดแล้ว
- 2026-09-08 — [แผนฉบับ CouchDB แทน PostgreSQL](couchdb-revision.md) — อาจารย์สั่งเปลี่ยน DB → ADR-0012 (Proposed) + `06_COUCHDB_REVISION.md` + scrutinize รอบ 4 · freeze #4–#37 — ปิดแล้ว (ผล: Rejected ดูบรรทัดบน)
- 2026-09-07 — [merge #41/#42, lane → GitHub handle](merge-p1-p2-lane-assignments.md) — #14/#15 เข้า `main`, team/1–3 ผูกกับคนจริง — ปิดแล้ว
- 2026-09-07 — [reassign #14 → team/1](reassign-ticket-14.md) — ตาม commit author — ปิดแล้ว
- 2026-09-07 — [lane primer](lane-primer-breakdown.md) — `docs/00_LANE_PRIMER.md` สำหรับคนใหม่ — ปิดแล้ว
- 2026-09-07 — [reorganize root → frontend/ server/ docs/](monorepo-root-cleanup.md) — ปิดแล้ว
- 2026-09-06 — [#15 p2 schema + RLS + seed · #38 backend CI](p2-schema.md) — 27 ตารางเป็น migration, RLS forced — ปิดแล้ว
- 2026-09-06 — [#14 p1 compose stack, Nginx, health](p1-compose-stack.md) — ปิดแล้ว
- 2026-09-05 — [design → 30 GitHub issues + แบ่ง 3 ทีม](to-tickets-backend.md) — ปิดแล้ว
- 2026-09-04 — [grill รอบ 2 → ADR-0010/0011 + Flutter CI](grill-round2-ci.md) — ปิดแล้ว
- 2026-09-04 — [backend design grill → ADR + Drift schema v2](backend-design-adr.md) — ปิดแล้ว
- 2026-07-14 — [Riverpod → flutter_bloc](riverpod-to-bloc.md) — ปิดแล้ว
