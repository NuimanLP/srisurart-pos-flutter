# Handoff Log: Session 2026-09-19 — Tickets #274, #189, #273, #228 (Lane B Phase 2)

**Date**: 2026-09-19  
**Branch**: `lane2`  
**Commits Produced**:
- `0c2b6da`: `feat(numbering): pos device issues RC/CN numbers locally (Closes #274)`
- `1dff9a9`: `feat(numbering): guard offline doc generation when seed marker is missing (Closes #189)`
- `61143b3`: `feat(pwa): implement PWA shell with offline SW, web lock, and single-tab protection (Closes #273)`
- `b7c1513`: `feat(sync): outbox_ops and SyncService engine with aggregate head-of-line blocking (Closes #228)`
- `f56e904`: `chore(drift): regenerate database.g.dart for schema v9 (outbox_ops)`

---

## 1. Overview of Work Completed

During this session, we sequenced and delivered all tickets in **Phase 2 Stage 2 (Doc Numbering & PWA Shell)** and **Phase 2 Stage 3 (Core Outbox & SyncService Engine)** assigned to Lane B (`team/2`, `LomerAlloys`):

### 1.1 Analyzer Warnings Resolution
- Fixed 4 `unawaited_return_in_try_block` warnings across [`api_customers_repository.dart`](../../frontend/lib/data/repositories/api_customers_repository.dart), [`api_mechanics_repository.dart`](../../frontend/lib/data/repositories/api_mechanics_repository.dart), and [`api_products_repository.dart`](../../frontend/lib/data/repositories/api_products_repository.dart), ensuring `dart analyze` baseline was 100% clean (0 issues).

### 1.2 Ticket #274 (Slice 4-c: `num.1-client` — เครื่อง pos ออกเลข RC/CN เอง)
- **Status**: Completed & Closed.
- **Components**: [`doc_number_service.dart`](../../frontend/lib/data/services/doc_number_service.dart), [`database.dart`](../../frontend/lib/data/db/database.dart).
- **Core Invariants**:
  - Local document numbers formatted as `{prefix}{deviceNo:02d}-{period}-{seq:04d}` (e.g. `RC01-2569-09-0001`, `CN01-2569-09-0001`).
  - Period derived strictly from local device clock as Buddhist Era `YYYY-MM` (`year + 543`).
  - Sequence resets to `0001` upon transitioning to a new month while offline.
  - `9999` exhaustion raises `DocNumberExhaustedException` (`DOC_NUMBER_EXHAUSTED`), never wrapping around.
  - Non-burning 4xx reuse: numbers are candidates until 2xx confirmation or outbox queue commitment.
  - Bumped Drift schema to **v8**: wiped stale `doc_counter_seeds` upon upgrade (C16).
- **Tests**: 15 unit tests in [`doc_number_service_test.dart`](../../frontend/test/doc_number_service_test.dart).

### 1.3 Ticket #189 (Slice 5: ห้ามออกเลขออฟไลน์เมื่อเครื่องยังไม่มี seed marker)
- **Status**: Completed & Closed.
- **Components**: [`doc_number_service.dart`](../../frontend/lib/data/services/doc_number_service.dart).
- **Core Invariants**:
  - Guards offline document generation (`isOffline: true`). If device has no marker in `doc_counter_seeds` for the current `(deviceId, period)`, it rejects immediately before write or print with `OfflineSeedRequiredException` and Thai message: `"ต้องเชื่อมต่ออินเทอร์เน็ตหนึ่งครั้งเพื่อเตรียมเลขเอกสารก่อนใช้งานออฟไลน์"`.
  - Online generation and seeded offline generation operate smoothly.
- **Tests**: 8 additional unit tests (total 23) in [`doc_number_service_test.dart`](../../frontend/test/doc_number_service_test.dart).

### 1.4 Ticket #273 (Slice 3: `pwa.1` — PWA shell)
- **Status**: Completed & Closed.
- **Components**: [`sw.js`](../../frontend/web/sw.js), [`index.html`](../../frontend/web/index.html), [`manifest.json`](../../frontend/web/manifest.json).
- **Core Invariants**:
  - Offline-first Service Worker precaching shell assets (`sqlite3.wasm`, `drift_worker.js`, `index.html`, etc.), with API requests (`/api/*`) bypassing SW cache.
  - Strict update invariant: No automatic `self.skipWaiting()` during install to prevent tab reloads mid-sale. Prompts user via banner before updating.
  - Persistent storage requested via `navigator.storage.persist()`.
  - Single-tab exclusive lock via `navigator.locks.request('srisurart-pos-writer')`. If unavailable after 2s retry window, aborts Flutter boot and displays styled Thai "เปิดอยู่แล้ว" screen.
- **Tests**: 14 tests in [`pwa_shell_test.dart`](../../frontend/test/pwa_shell_test.dart).

### 1.5 Ticket #228 (Slice 8-c: `outbox_ops` + `SyncService` + State Machine + Head-of-Line Blocking)
- **Status**: Completed & Closed.
- **Components**: [`tables.dart`](../../frontend/lib/data/db/tables.dart), [`database.dart`](../../frontend/lib/data/db/database.dart), [`database.g.dart`](../../frontend/lib/data/db/database.g.dart), [`sync_service.dart`](../../frontend/lib/data/sync/sync_service.dart), [`repository_providers.dart`](../../frontend/lib/presentation/repositories/repository_providers.dart).
- **Core Invariants**:
  - Drift schema **v9**: table `outbox_ops` with 11 columns.
  - Atomic write rule (ADR-0010 / #84): business entities and outbox ops written in a single SQLite transaction (`writeAtomic`), avoiding legacy Drift transactional services.
  - `SyncService` implementing `SyncFacade`:
    - 3-State Machine: `Online`, `Degraded`, `Syncing`. Degraded triggered by 3 consecutive health probe failures, >5s timeout, or non-verdict write. 4xx is treated as a verdict and does NOT trigger Degraded.
    - Single-flight push loop batching up to 50 operations to `POST /api/v1/sync/push` with header `X-Device-Token` and `outboxRemaining`.
    - Head-of-line blocking (§8.4): 3 non-verdict failures mark op as `stuck`. Ops sharing the stuck aggregate wait; independent aggregates proceed.
    - Implemented `resend` and `discard` methods (with `serverHasRow: false` cleaning up uncommitted local rows).
    - Replaced `NullSyncFacade` in `repository_providers.dart`.
- **Tests**: 13 comprehensive tests in [`sync_service_test.dart`](../../frontend/test/sync_service_test.dart).

### 1.6 CI Drift Codegen Fix
- Resolved GitHub Actions `flutter.yml` failure on `git diff --quiet -- '*.g.dart'`.
- Ran `dart run build_runner build` on an isolated ASCII directory (`D:\srisurart_codegen\frontend`) as required by `AGENTS.md` to bypass the non-ASCII AOT path compiler limitation.
- Synchronized generated `database.g.dart` (+444 / -113 diff) and pushed commit `f56e904`.

---

## 2. Current State of the Codebase

- **Branch**: `lane2` (synchronized with `origin/lane2`).
- **Working Tree**: Clean.
- **Drift Schema Version**: `9`.
- **Static Analysis**: `dart analyze` in `frontend/` reports **0 issues**.
- **Test Suite**: `flutter test` reports **433 / 433 tests passing**.
- **Closed Issues on GitHub**: #272, #266, #274, #189, #273, #228.

---

## 3. Next Steps (Remaining Phase 2 Tickets)

The next agent should proceed with **Stage 4: Downstream Offline Business Logic** running on top of the newly implemented Outbox Engine:

1. **Ticket #194 (Slice 14-c: `override วงเงินตอนออฟไลน์`)**:
   - Smallest slice: when cashier confirms credit limit warning offline, carry `overrideCreditLimit: true` in the `sale.create` outbox payload.
2. **Ticket #275 (Slice 9: `ย้ายชำระเครดิตเข้า outbox_ops`)**:
   - Migrate `pending_credit_payments` table to use `outbox_ops`.
3. **Ticket #276 (Slice 11-c: `ปุ่ม void ออฟไลน์เฉพาะบิลที่ขายออฟไลน์ + เหตุผล`)**:
   - Add `Sales.soldOffline` in Drift schema (next bump v10), allow offline void only for offline bills, require reason, push `sale.void_offline`.
4. **Ticket #211 (Slice 10: `PIN ออฟไลน์บนเครื่อง pos`)**:
   - Store slow PIN hash in Drift, valid for 3 days from `/auth/token`, verify locally in Degraded mode only, lock after 5 failures.
5. **Ticket #229 (Slice 12: `ลูกค้าเข้าคิว · ปิดปุ่มออนไลน์เท่านั้น · ลบ fallback ของ api_*.dart`)**:
   - Queue customer writes, disable online-only buttons in Degraded, remove all 22 `super.<write>()` fallbacks.
6. **Ticket #212 (Slice 13b: `pull: cursor ของ server, ถอย 30 วินาที, ไม่ทับสต็อกใต้ op ค้าง`)**:
   - Implement cursor-based pull sync, 30s lookback, protect local stock from overwrite if op is pending.
7. **Ticket #193 (Slice 20-c: `contract test ของ SyncService กับ fake server`)**:
   - Contract test suite in CI (`flutter.yml`) against all fixtures in `docs/Backend_design/fixtures/sync-push/`.

---

## 4. Suggested Skills for the Next Agent

- **`token-efficient-dev`**: Always activate for parent/subagent token conservation rules, zero-polling reactive wakeup, and strict negative constraints.
- **`domain-modeling`**: Useful if consulting or updating ADRs, domain aggregates, or schema contracts.
